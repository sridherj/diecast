# Test Inventory — `cast-server/` (Phase 3b sp1 fork)

Generated by Phase 3b sub-phase 1 (`sp1_fork`). This file enumerates the test
posture of the freshly-forked tree and dispositions every test + every
non-test artifact that will need attention in later sub-phases. **Nothing
below has been edited yet** — this is a pre-edit ledger, per the no-edits
constraint of sp1.

## Source of the fork

- **Source:** `/data/workspace/second-brain/taskos/src/taskos/`
- **Destination:** `/data/workspace/diecast/cast-server/`
- **Method:** `cp -R` then prune `__pycache__/`, `*.pyc`, `.pytest_cache/`,
  `.mypy_cache/` (zero of each remain — verified).

## Tests carried in this fork

| Test path | Purpose | Disposition |
|-----------|---------|-------------|
| _(none)_ | The TaskOS source tree (`taskos/src/taskos/`) is the runtime package only — tests live outside the package under `taskos/tests/` in the source monorepo and are **not** part of `cp -R src/taskos`. | n/a — no inherited test files |

A full `find cast-server -name 'test_*.py' -o -name '*_test.py' -o -name
'conftest.py'` returns zero results. `cast-server/tests/` does not exist.

### Implication for downstream sub-phases

- **sp6** (rate-limit auto-restart regression test) and **sp6_5** (9-concurrent
  cap regression test) author **new** tests in a fresh `cast-server/tests/`
  directory. They are not "rewriting" existing tests — they are originating
  the suite.
- The TaskOS tests in the source monorepo (`taskos/tests/`) are **not**
  carried; they remain in second-brain and continue to cover that codebase.
  Per the Phase 3b plan, regression coverage on the Diecast side is
  re-originated in sp6/sp6_5.
- sp2's stub-prune therefore has no test breakage to manage in this tree.

## Non-test artifacts requiring attention later

Enumerated per the sp1 plan §Step 3 ("non-test artifacts that may need
attention"). Disposition column points at the sub-phase that owns the change.

### Modules to be stubbed in sp2 (`NotImplementedError`) and deleted in sp13

| Path | Reason | Owner |
|------|--------|-------|
| `news_digest/__init__.py` | Out-of-scope for OSS Diecast (news pipeline pruned). | sp2 → sp13 |
| `news_digest/runner.py` | Same. | sp2 → sp13 |
| `goal_detector/__init__.py` | Out-of-scope (goal-detection pipeline pruned). | sp2 → sp13 |
| `goal_detector/detector.py` | Same. | sp2 → sp13 |
| `goal_detector/runner.py` | Same. | sp2 → sp13 |
| `sync/__init__.py` | Out-of-scope (sync engine pruned, A5 plan-review fix: must raise `NotImplementedError`, no silent no-op). | sp2 → sp13 |
| `sync/engine.py` | Same — explicit A5 callout. | sp2 → sp13 |
| `sync/parsers/` (subtree) | Same. | sp2 → sp13 |
| `routes/api_news.py` | Routes the pruned news pipeline. | sp2 → sp13 |
| `routes/api_sync.py` | Routes the pruned sync engine. | sp2 → sp13 |
| `templates/pages/news.html` | UI for the pruned news pipeline. | sp2 → sp13 |

The lifespan + middleware that wire these modules are removed in sp2 step 1
per the shared-context note, so the stubs exist only to keep import statements
live until sp13's deletion pass.

### Modules carried verbatim (no edit in 3b apart from rebrand sweep)

| Path | Notes |
|------|-------|
| `__init__.py` (11 of these across the tree) | Package markers — carry. |
| `app.py` | FastAPI app entry. sp2 strips news/sync wiring; sp3/sp3_5 rebrand strings + module path. |
| `config.py` | Carries verbatim; sp7 adds the `~/.cast/config.yaml` thin reader. |
| `deps.py` | Carries verbatim. |
| `design-system.md` | Carries — sp4a consumes for token rebrand. |
| `db/` | Schema + migrations — Phase 3a already landed `estimate_size`. |
| `infra/` | **`infra/tmux_manager.py` is owned by Phase 3a sp2; do NOT touch in 3b.** Other infra files carry. |
| `models/` | SQLAlchemy / pydantic models — carry. |
| `routes/api_agents.py` | Carries; sp5 adds file-canonical read-through. |
| `routes/api_artifacts.py` | Carries. |
| `routes/api_goals.py` | Carries. |
| `routes/api_scratchpad.py` | Carries (KEEP per §7 selective-expansion lock). |
| `routes/api_task_suggestions.py` | Carries. |
| `routes/api_tasks.py` | Carries; sp8 adds the US10 L/XL "consider splitting" affordance hook. |
| `routes/pages.py` | Carries; sp3 rebrand + sp9 preso route addition. |
| `services/agent_service.py` | Carries. |
| `services/context_map.py` | Carries. |
| `services/error_memory_service.py` | Carries. |
| `services/goal_service.py` | Carries. |
| `services/orchestration_service.py` | Carries. |
| `services/scratchpad_service.py` | Carries (Scratchpad page is KEEP per §7). |
| `services/suggestion_service.py` | Carries. |
| `services/task_service.py` | Carries — Phase 3a sp4b already landed `estimate_size` field handling. |
| `services/task_suggestion_service.py` | Carries. |
| `templates/` | Pages + fragments + macros — sp3 rebrand strings; sp4a CSS chrome; sp4b EasyMDE rebrand. |
| `static/` | CSS + vendored JS + assets — sp4a/sp4b own edits. |
| `utils/` | Helper modules — carry. |

### Database / data files

| Path | Disposition |
|------|-------------|
| `data/taskos.db` | **Not present** in the package source (lives in repo root `data/` in second-brain, not under `src/taskos/`). Nothing to drop in this fork. Diecast persists to `~/.cast/diecast.db` per Phase 3b §1, configured in sp7. |

A `find cast-server -type f \( -name '*.db' -o -name '*.sqlite' -o -name
'*.sqlite3' \)` returns zero results — confirmed clean.

## Verification snapshot (sp1 §Verification)

- `ls cast-server/app.py services/agent_service.py routes/api_agents.py` →
  all three files present.
- `find cast-server -name __pycache__` → 0 results.
- `find cast-server -name '*.pyc'` → 0 results.
- `find cast-server -name '.pytest_cache'` → 0 results.
- `find cast-server -name '.mypy_cache'` → 0 results.

## Handoff

- **sp2** uses the "Modules to be stubbed" table above as its cut list.
- **sp3** uses the "Modules carried verbatim" table to scope its rebrand
  string sweep.
- **sp6 / sp6_5** know the test suite is empty in this tree and will originate
  it from scratch.
