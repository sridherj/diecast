name: cast-ui-test-requirements-render
description: |
  Diecast UI e2e: requirements-render comment flows (Phase 4). Select→pill→composer→mark,
  resolve, version toggle→diff, displaced→tray — via Playwright.
entrypoint: cast-ui-test-requirements-render.md
inputs:
  goal_slug:
    type: string
    required: true
  base_url:
    type: string
    required: false
    default: http://127.0.0.1:8006
tags:
  - test
  - ui
visible: true
