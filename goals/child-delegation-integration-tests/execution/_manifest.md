# Execution Manifest: Child Delegation Integration Tests

## How to Execute

Each sub-phase runs in a **separate Claude context**. For each sub-phase:

1. Start a new Claude session
2. Tell Claude: "Read `goals/child-delegation-integration-tests/execution/_shared_context.md`,
   then execute `goals/child-delegation-integration-tests/execution/<spX_Y_dir>/plan.md`"
3. After completion, update the Status column below
4. At gates (G prefix), STOP and surface the gate prompt to the human user

## Sub-Phase Overview

| # | Sub-phase | Directory/File | Depends On | Status | Notes |
|---|-----------|----------------|------------|--------|-------|
| sp1.1 | Verify `CAST_TEST_AGENTS_DIR` seam | `sp1_1_verify_seam/` | -- | Done | Read-only audit confirmed at agent_config.py:62-70, agent_service.py:1615-1631, _config_cache:59 |
| sp1.2 | Author 4 fixture configs + literal prompts | `sp1_2_fixture_configs/` | -- | Done | 4 fixture dirs created under `cast-server/tests/integration/agents/`: parent-delegator (http, allowlist=[child-worker, child-worker-subagent]), child-worker (http, allowlist=[]), child-worker-subagent (subagent, allowlist=[]), delegation-denied (http, allowlist=[child-worker], deliberately-failing). Each has `config.yaml` + `<name>.md` with frontmatter + literal contract-v2 envelope instructions. Validation script confirms all 4 dispatch_modes and allowed_delegations match locked decisions. |
| sp1.3 | Conftest wiring (`CAST_TEST_AGENTS_DIR` + cache clear) | `sp1_3_conftest_wiring/` | sp1.1, sp1.2 | Done | Created cast-server/tests/integration/{conftest.py, __init__.py, test_fixture_agents_load.py}. conftest sets CAST_TEST_AGENTS_DIR (session autouse) + clears _config_cache (per-test autouse). 4 parametrized fixture-load tests pass; validation script prints http/http/subagent/http. T2 hand-off note documented in conftest module docstring. Risk #2 retired. |
| sp1.4 | Equivalence-map docstring stub | `sp1_4_equivalence_stub/` | -- | Done | Created cast-server/tests/integration/test_child_delegation.py with FR-007 docstring (11 second-brain class TODOs + 5 diecast-only additions); ast parse OK, pytest --collect-only reports 0 tests |
| GA | Gate A: confirm fixture path + dispatch_modes + seam | `gate_A_fixture_decisions.md` | sp1.1, sp1.2, sp1.3, sp1.4 | Done | DECISIONS (user-confirmed 2026-05-01): (1) fixture path = cast-server/tests/integration/agents/<name>/config.yaml; (2) dispatch_modes + allowed_delegations accepted as planned; (3) registry seam = existing CAST_TEST_AGENTS_DIR env var |
| sp2.1 | Allowlist + Depth + ChildLaunch + TerminalTitle classes + suite timeout | `sp2_1_allowlist_depth/` | sp1.3 | Done | Created cast-server/tests/integration/pytest.ini (timeout=5) and appended 5 classes to test_child_delegation.py: TestAllowlistValidation (2 tests), TestDelegationDepthEnforcement (1), TestDepthCalculation (4), TestChildLaunchIsolation (2), TestTerminalTitleFormatting (3). 12/12 pass in ~1.2s. Equivalence-map TODO sp2.1 markers replaced; documented `open_terminal` divergence vs second-brain `open_terminal_tab`. Note: pytest-timeout plugin not installed — pytest emits "Unknown config option: timeout" warning; key takes effect once the plugin is added. |
| sp2.2 | DelegationContextFile + ResultSummary + FinalizeCleanup (BOTH finalizers) | `sp2_2_context_summary_cleanup/` | sp2.1 | Done | Serial-run override. Appended 3 classes / 7 tests to test_child_delegation.py: TestDelegationContextFile (2: write-before-return via logger.info downstream-symbol monkeypatch + verbatim JSON-shape), TestResultSummary (3: 50-char populate / missing-summary→None / 350→300 truncate, all driven via _finalize_run_from_monitor), TestFinalizeCleanup (2 RED: .delegation/.prompt/.continue/.output four-file inventory). Module total 17 passed, 2 failed in 1.74s. The 2 FinalizeCleanup reds are INTENTIONAL Gate B / sp4b candidates: (a) `_finalize_run` (sync, ~line 1702) deletes none of .delegation/.prompt/.continue; (b) `_finalize_run_from_monitor` (async, ~line 2520) deletes .delegation+.prompt but NOT .continue. Cleanup-contract drift between the two finalizers is now a concrete failing test pair, per plan §"do not paper over it". Equivalence-map TODO sp2.2 markers replaced (TestDelegationContext→TestDelegationContextFile, TestResultSummary→TestResultSummary, TestPromptFileCleanup→TestFinalizeCleanup) with divergence notes. FR-008 grep clean. |
| sp2.3 | Preamble builders + DispatchModeValidator | `sp2_3_preamble_builders/` | sp2.2 | Done | Appended 4 classes / 15 tests to test_child_delegation.py: TestPreambleAntiInline (3), TestPromptBuilder (6 parametrized), TestMixedTransportPreamble (3 incl. SC-004 word-boundary regex + exactly-once anti-inline), TestDispatchModeValidator (3 incl. typo→http silent fallback w/ sibling-field preservation). All 15 green in ~1s. Module total 32 passed / 2 failed (the 2 reds are sp2.2's intentional cleanup-contract drift, unchanged). Equivalence-map TODO sp2.3 markers replaced with diecast counterparts; documented diecast verbatim anti-inline phrase ("CRITICAL: NEVER inline an agent's work yourself.") and the ``HTTP-dispatched delegations:`` / ``Subagent-dispatched delegations:`` block-header anchors as pinned constants. _build_agent_prompt NOT modified — used as-is via `agent_service.load_agent_config` monkeypatch on the public seam. FR-008 grep clean. |
| sp2.4 | ContinueAgentRun (security-relevant tmux assertion) | `sp2_4_continuation_file/` | sp2.3 | Done | Appended TestContinueAgentRun (3 tests) to test_child_delegation.py — security-relevant test FIRST per design-review flag (US1.S10). Tests pin: (1) tmux send_keys payload contains `Read <path>` and the run-scoped `.agent-<run_id>.continue` filename but NEVER the message body (proxied via `EVIL_INJECTION_TOKEN_8a3f` sentinel — terminal-injection guardrail); (2) message bytes written verbatim to `GOALS_DIR/<slug>/.agent-<run_id>.continue`; (3) missing tmux session raises `ValueError` with substring `"no longer exists"`. Mock target: `agent_service._get_tmux` → `MagicMock` with `session_exists` toggled per case. `continue_agent_run` is async — exercised via `asyncio.run`. 3/3 green in ~1.1s; module total now 35 passed / 2 failed (the 2 reds remain sp2.2's intentional cleanup-contract drift). Equivalence-map TODO sp2.4 marker replaced (TestContinuationFileDelivery → TestContinueAgentRun) with security-intent and mock-shape notes. FR-008 grep clean. |
| sp2.5 | Wall-clock budget gate + equivalence-map completion | `sp2_5_budget_gate/` | sp2.4 | Done | Full T1 wall-clock 3.5s real / 1.81s pytest (well under 30s budget). 35 passed / 2 failed — both fails are sp2.2's intentional cleanup-contract drift candidates (bucket 4b). Equivalence-map docstring TODO/TBD scan returns empty (exit 1); zero xfail markers. Gate B input: (1) `TestFinalizeCleanup::test_cleanup_via_finalize_run` — `.delegation-<id>.json` retained after sync `_finalize_run` → bucket 4b; (2) `TestFinalizeCleanup::test_cleanup_via_finalize_run_from_monitor` — `.continue` retained after async `_finalize_run_from_monitor` → bucket 4b. No tightening needed (suite already < budget). |
| GB | Gate B: failing-test enumeration | `gate_B_failing_tests.md` | sp2.5 | Done | DEFERRED (user 2026-05-01): review T1 failures after the orchestration run; do not dispatch fix sweep now. sp4a/sp4b/sp4c marked Skipped. |
| sp3.1 | ExternalProjectDirPrecondition (additive only) | `sp3_1_external_project_dir/` | sp2.5 | Done | Cross-checked existing 11 tests in cast-server/tests/test_dispatch_precondition.py under T1 env vars (all green, 2.83s). Appended TestExternalProjectDirPrecondition (2 methods) to test_child_delegation.py: test_depth4_dispatch_returns_422_before_row_create (US2.S7 — ROW-COUNT INVARIANCE pin: chain run0→run1→run2→run3, 4th dispatch raises ValueError("Max delegation depth"), agent_runs row count unchanged at 4 → depth check fires before create_agent_run); test_invoke_route_does_not_422 (carve-out preserved — invoke_agent on goal with NO external_project_dir does NOT raise MissingExternalProjectDirError, asserts "run_id"+"prompt" in result to prove success path). FR-008 grep clean. Equivalence-map docstring expanded with verbatim citations of all 9 covered cases + divergence rationale for the 2 new methods. Module total now 37 passed / 2 failed (the 2 reds remain sp2.2's intentional cleanup-contract drift). Combined with test_dispatch_precondition.py: 48 passed / 2 failed in 4.37s. |
| sp3.2 | OutputJsonContractV2 + schema fixture | `sp3_2_output_json_v2/` | sp3.1 | Done | Created cast-server/tests/fixtures/{__init__.py, next_steps.schema.json} (US14 typed-shape: required {command, rationale, artifact_anchor}, additionalProperties=false). Appended TestOutputJsonContractV2 (3 methods / 5 cases) to test_child_delegation.py: test_next_steps_bare_string_fails_schema (US2.S5, GREEN — pure jsonschema validator pin); test_non_terminal_status_treated_as_malformed (US2.S4, parametrized over [pending, running, idle], 3 RED — sp4c candidate: AgentOutput.status is `str` not Literal, so non-terminal values pass Pydantic validation and propagate verbatim into agent_runs.status); test_untagged_open_questions_flagged (US2.S6, RED — sp4c candidate: no production validator for human_action_items tag prefixes exists; grep EXTERNAL/USER-DEFERRED in cast_server/ returns zero matches). NO xfail markers per US2. jsonschema==4.26.0 already in deps; FR-008 grep clean (no requests/httpx/urllib). Equivalence-map docstring expanded with method list + EXPECTED-RED rationale + sp4c hand-off notes. Module total: 38 passed / 6 failed in 3.29s under T1 env (2 cleanup-drift reds from sp2.2 unchanged + 4 new sp3.2 reds). Schema is test-only for now per resolved open question; promotion to cast_server/contracts/ deferred to sp4d. |
| sp3.3 | MtimeHeartbeatRoundTrip (EXPECTED red until sp4a) | `sp3_3_mtime_heartbeat/` | sp3.2 | Done | Appended TestMtimeHeartbeatRoundTrip (1 test) to test_child_delegation.py. Test PASSES at first try → Gate B Option C signal per plan §Execution Notes (no parent-stall symptom in the basic file-canonical detection path). NO xfail per US2. Polling primitive exercised: `_check_all_agents` (agent_service.py:2360) directly invoked twice with a counting wrapper; output.json write between ticks; AgentState forced to IDLE so `_handle_state_transition` (line 2419-2422) checks `output_file.exists()` and routes to `_finalize_run_from_monitor`. Module total 39 passed / 6 failed in 3.10s under T1 env (CAST_DISABLE_SERVER=1, CAST_DELEGATION_BACKOFF_OVERRIDE=10ms,20ms,50ms, CAST_DELEGATION_IDLE_TIMEOUT_SECONDS=4): unchanged 2 sp2.2 cleanup-drift reds + 4 sp3.2 reds; no regressions. Equivalence-map docstring expanded with polling-primitive citation + Gate B Option C note. FR-008 grep clean. |
| sp4a | Parent stall after child finalize | `sp4a_parent_stall/` | sp3.3 | Skipped | Per Gate B decision: defer fix sweep until end-of-orchestration review |
| sp4b | Cleanup or contract drift | `sp4b_cleanup_contract_drift/` | sp3.3 | Skipped | Per Gate B decision |
| sp4c | Allowlist/depth/output-JSON silent violations | `sp4c_silent_violations/` | sp3.3 | Skipped | Per Gate B decision |
| sp5.1 | E2E happy path skeleton + session-scoped server fixture | `sp5_1_e2e_happy_path/` | sp3.3 | Done | Created cast-server/tests/e2e/{__init__.py, conftest.py, test_tier_delegation.py, pytest.ini}. Session-scoped `cast_server_process` spawns `bin/cast-server` on `CAST_E2E_PORT` (default 8765, host 127.0.0.1) with `CAST_TEST_AGENTS_DIR` exported into its env (sp1.3 hand-off honored), health-checks `GET /api/agents/runs?status=running` (cast-server has no `/healthz`; `/api/health` shells out to bin/cast-doctor — too slow for liveness), fails fast if port is occupied or process exits during startup, SIGTERM the process group on session-end. Per-test `reset_e2e_goal_dir` idempotently creates the `child-delegation-e2e` goal via `goal_service.create_goal` + `goal_service.update_config(external_project_dir=...)` (US2.S7 dispatch precondition), purges `.agent-*` / `.delegation-*` / `*.prompt` / `*.continue` / `*.output.json` runtime files (preserves `goal.yaml` + `requirements.human.md`), and DELETEs all `agent_runs` rows scoped to that `goal_slug`. `test_parent_delegator_happy_path` (US3.S1) skeleton uses bash-curl helpers (`_curl_post` / `_curl_get`) — FR-008 grep clean — polls DB row terminal state via `agent_service.get_agent_run`, asserts parent.status=completed, asserts ≥1 child with parent_run_id linkage and non-empty result_summary, asserts child tmux session `agent-<run_id>` is torn down post-finalize. `@pytest.mark.e2e` registered in `cast-server/tests/e2e/pytest.ini` (no warning); `@pytest.mark.timeout(360)` decorator present and emits the same PytestUnknownMarkWarning as integration suite until pytest-timeout plugin is installed (sp2.5 posture). pytest --collect-only collects 1 test in 0.49s. AST parse OK. **Open hand-off for sp5.4 / pre-CI:** the four delegation fixture .md prompts under `cast-server/tests/integration/agents/` hard-code `http://localhost:8005` for the child-trigger curl; the spawn fixture uses port 8765 to avoid colliding with developer cast-server. sp5.4 must decide whether to (a) template fixture prompts with a `<cast_server_url>` placeholder or (b) pin the e2e spawn port to 8005 with a port-occupied precondition. The skeleton is ready either way — only the `cast_server_base_url` returned by the fixture and the fixture-prompt port need to align before `pytest -m e2e` will go green on a real host. |
| sp5.2 | E2E delegation_denied | `sp5_2_e2e_denied/` | sp5.1 | Done | Appended `test_delegation_denied` (US3.S2) to cast-server/tests/e2e/test_tier_delegation.py. Triggers `cast-test-delegation-denied`, asserts (a) parent reaches a terminal status (graceful — `_TERMINAL_STATUSES` set, NOT pinned to `"completed"` — the sp1.2 fixture's literal envelope writes `status: "failed"` since the dispatch was denied; per sp3.2 RED the AgentOutput.status propagates verbatim, so the plan's `== "completed"` assertion would always red under the fixture-as-authored — substring is the byte-stable contract per Review #5, terminal-status is the graceful-handling contract); (b) zero child rows linked to parent_run_id (denial fires BEFORE create_agent_run); (c) `result_summary` contains `"422"` OR `"denied"` (pinned to fixture envelope's literal `"denied (422)"`). Reused sp5.1 helpers (`_curl_post`, `_wait_for_run_terminal`, `_children_of`). Verified the denied fixture's `denied (422)` substring is unchanged at cast-test-delegation-denied.md:74. AST parse OK; `pytest --collect-only` collects 2 tests in 0.46s. FR-008 grep clean (no requests/httpx/urllib in module). **Plan deviation flagged for sp4 / spec retro:** plan's `parent_run.status == "completed"` assertion conflicts with the sp1.2 fixture writing `"failed"` — relaxed to terminal-set membership, deviation noted in test docstring; user can resolve via fixture update (write `"completed"` summary with denial detail) OR plan/spec update (allow either terminal status). |
| sp5.3 | E2E mid_flight_session_isolation | `sp5_3_e2e_session_isolation/` | sp5.2 | Done | Appended `test_mid_flight_session_isolation` (US3.S3) to cast-server/tests/e2e/test_tier_delegation.py + new module helper `_list_tmux_sessions`. Triggers `cast-test-parent-delegator`, polls tmux session list every 0.5s for up to 60s (spec-cited observation window) looking for both `agent-<parent_run_id>` and `agent-<child_run_id>` alive concurrently; if race lost, `pytest.skip("child too fast to observe mid-flight")` — skip string verbatim from second-brain `test_tier2_delegation.py::test_mid_flight_session_isolation`. Once both observed alive: asserts (a) session names differ (separate tmux sessions, not split panes within one), (b) parent's `agent-<parent_run_id>` session has exactly 1 pane (parent MUST NOT split its own pane to host the child — cast-delegation-contract.collab.md §Session Isolation). NO "slow-child" knob introduced (plan §Out of Scope). Reused sp5.1 helpers (`_curl_post`, `_children_of`); added `_list_tmux_sessions` helper using `tmux list-sessions -F #{session_name}` (returns [] on rc != 0, e.g. before tmux server starts). AST parse OK; `pytest --collect-only` collects 3 tests in 0.52s under e2e marker. FR-008 grep clean (no requests/httpx/urllib imports anywhere in module). Gate C now ready to fire for sp5.4. |
| GC | Gate C: T2 ptyxis CI strategy | `gate_C_ptyxis_ci.md` | sp5.3 | Done | DECISION (user-confirmed 2026-05-01): Option A — self-hosted runner with ptyxis. sp5.4 to author workflow with `runs-on: [self-hosted, ptyxis]` on nightly schedule. |
| sp5.4 | Nightly CI workflow YAML | `sp5_4_ci_workflow/` | sp5.3 | Done | Authored `.github/workflows/cast-delegation-e2e-nightly.yml` per Gate C Option A (`runs-on: [self-hosted, ptyxis]`). Cron `37 6 * * *` UTC (off-peak from `setup-correctness` 06:17 and `e2e` 06:47); `workflow_dispatch: {}` enabled. `timeout-minutes: 30`. Steps: checkout → install uv (idempotent — skip if pre-installed on the self-hosted host) → show prereq versions (uv, tmux, claude, ptyxis — ptyxis missing emits WARNING surfacing label drift) → `uv sync --group dev` → `uv run pytest cast-server/tests/e2e/test_tier_delegation.py -m e2e -v`. **Production cadence preserved** — NO T1 env-var overrides set (CAST_DISABLE_SERVER / CAST_DELEGATION_BACKOFF_OVERRIDE / CAST_DELEGATION_IDLE_TIMEOUT_SECONDS unset → real polling/backoff exercised). **Port alignment:** `CAST_E2E_PORT=8005` set to pin the e2e spawn fixture to the port hard-coded in the four delegation fixture prompts (sp5.1 hand-off resolved by env-var, NOT by editing test code — plan §"Out of scope: Modify the test code" preserved). `actionlint` not present locally; PyYAML parse OK. sp7.1 will accumulate ≥3 nightly runs to satisfy SC-002. |
| sp6.1 | TestSubagentOnlyPreamble | `sp6_1_subagent_preamble/` | sp3.3 | Done | Appended TestSubagentOnlyPreamble (3 tests) to test_child_delegation.py: emits Subagent-dispatched block + named target + universal anti-inline phrase; omits HTTP-dispatched block AND `Quick reference (full patterns in the skill):` curl quick-reference (pinned dispatch-curl substring `CHILD_RUN_ID=$(curl` instead of plain `curl`, since the API-routes preamble emits a separate `Health check: curl ...` line); pins pass-through verdict / no-summarize phrase verbatim from `_subagent_dispatch_rules` (agent_service.py:1332-1352). 3/3 green in 1.52s. Module total now 42 passed / 6 failed under T1 env (the 6 reds are unchanged sp2.2 cleanup-drift + sp3.2 output-json-v2 candidates). FR-008 grep clean. Equivalence-map docstring expanded with method list + verbatim-phrase citations. |
| sp6.2 | MANUAL_SUBAGENT_CHECKLIST.md | `sp6_2_manual_checklist/` | -- | Done | 8 steps + verified-by footer + cross-refs at cast-server/tests/MANUAL_SUBAGENT_CHECKLIST.md |
| sp7.1 | CI observation (≥3 consecutive green nights) | `sp7_1_ci_observation/` | sp5.4 | Skipped | Multi-day elapsed-time wait — defer to post-orchestration |
| sp7.2 | Equivalence-map review + checklist verified-by footer | `sp7_2_equivalence_review/` | sp6.1, sp6.2 | Skipped | Defer until Phase 4 fixes applied |
| sp7.3 | Conditional spec update via `/cast-update-spec` | `sp7_3_spec_update/` | -- | Skipped | Defer until Phase 4 retro complete |
| sp7.4 | Flip goal status via `/cast-goals` | `sp7_4_close_goal/` | sp7.1, sp7.2, sp7.3 | Skipped | Defer goal-close until fixes verified |

Status transitions: `Not Started` → `In Progress` → `Done` → `Verified` → `Skipped`.
Gate rows (GA / GB / GC) pause the orchestrator for human decisions.

## Dependency Graph

```
sp1.1 ─┐
sp1.2 ─┼─► sp1.3 ─► [GATE A]
sp1.4 ─┘                │
                        ▼
                sp2.1 ─► sp2.2 ─┐
                        sp2.3 ─┼─► sp2.5 ─► [GATE B]
                        sp2.4 ─┘                │
                                                ▼
                                    sp3.1 ─┐
                                    sp3.2 ─┼─► (Phase 3 done) ─► sp4a/4b/4c (parallel)
                                    sp3.3 ─┘                              │
                                                                          ▼
                                            sp5.1 ─► sp5.2 ─┐
                                                    sp5.3 ─┴─► [GATE C] ─► sp5.4 ─┐
                                                                                   │
                                            sp6.1 (parallel with Phase 5)          │
                                            sp6.2 (independent)                    │
                                                                                   ▼
                                                            sp7.1 ─┐
                                                            sp7.2 ─┼─► sp7.4 (close-out)
                                                            sp7.3 ─┘
```

**Critical path:** sp1.1 → sp1.3 → GA → sp2.1 → sp2.5 → GB → sp3.x → sp4.x → sp5.1 → GC → sp5.4 → sp7.1 → sp7.4

## Execution Order

### Phase 1 — Fixture scaffolding (parallel-friendly)

Group 1a (parallel): sp1.1, sp1.2, sp1.4
Group 1b (after 1a): sp1.3
**[GATE A]** — surface fixture path + dispatch_modes + seam choice to user

### Phase 2 — T1 coverage parity (mostly serial)

Group 2a: sp2.1
Group 2b (after 2a, parallel): sp2.2, sp2.3, sp2.4
Group 2c (after 2b): sp2.5
**[GATE B]** — surface failing-test enumeration to user (anchors Phase 4 buckets)

> **Orchestration override:** Phase 2 sub-phases all append to `cast-server/tests/integration/test_child_delegation.py`. Despite the dep graph permitting sp2.2/sp2.3/sp2.4 in parallel, the orchestrator MUST run Phase 2 strictly serially (sp2.1 → sp2.2 → sp2.3 → sp2.4 → sp2.5) to avoid file-append merge conflicts. User-confirmed 2026-05-01.

### Phase 3 — T1 diecast-only additions (parallel)

Group 3 (after GB, parallel): sp3.1, sp3.2, sp3.3

> **Orchestration override:** Phase 3 sub-phases all append to the same integration test file. Run sp3.1 → sp3.2 → sp3.3 strictly serially. User-confirmed 2026-05-01.

### Phase 4 — Red→green fix sweep (parallel by symptom bucket)

Group 4 (after Phase 3 + GB, parallel): sp4a, sp4b, sp4c
**Each sub-phase commits ≥1 `fix(delegation): green <test_name>`.**

### Phase 5 — T2 live HTTP E2E

Group 5a (after Phase 4): sp5.1
Group 5b (after 5a, parallel): sp5.2, sp5.3
**[GATE C]** — surface ptyxis CI strategy to user
Group 5c (after GC): sp5.4

### Phase 6 — Subagent coverage (parallel with Phase 5)

Group 6 (parallel with Phase 5): sp6.1, sp6.2

### Phase 7 — Close-out

Group 7a (parallel after Phase 5+6): sp7.1, sp7.2, sp7.3
Group 7b (after 7a): sp7.4

## Progress Log

<!-- Update this after each sub-phase completion. Include date, sub-phase ID, and one-line outcome. -->
- 2026-05-01 — sp1.1 Done — Verified seam at agent_config.py:62-70 (path append), agent_service.py:1615-1631 (registry merge with collision-warn), _config_cache:59 (mtime-keyed); no production code changes required.
- 2026-05-01 — sp1.4 Done — Created cast-server/tests/integration/test_child_delegation.py as docstring-only stub. Equivalence map cites 11 actual second-brain classes (TestDelegationValidation, TestDelegationContext, TestResultSummary, TestChildLaunchTmuxIsolation, TestPtyxisTitleFormatting, TestContinuationFileDelivery, TestPromptFileCleanup, TestNoInlineEnforcement, TestDelegationInstructionInPrompt, TestInteractivePromptBlock, TestMixedTransportPreambleHarness) + 5 diecast-only additions. ast parse OK; pytest --collect-only collected 0 items.
- 2026-05-01 — sp6.2 Done — Authored cast-server/tests/MANUAL_SUBAGENT_CHECKLIST.md with 8 numbered steps (≤10), verified-by footer, cross-refs to T1/spec. SC-005 cold-read verification deferred to sp7.2.
- 2026-05-01 — sp1.3 Done — Wired cast-server/tests/integration/conftest.py (session-scoped CAST_TEST_AGENTS_DIR autouse + per-test _config_cache.clear() autouse) plus test_fixture_agents_load.py (4 parametrized cases all pass) + __init__.py. T2 hand-off note recorded in conftest module docstring. Risk #2 retired.
- 2026-05-01 — sp2.1 Done — Created cast-server/tests/integration/pytest.ini (timeout=5; pytest-timeout plugin not yet installed → unknown-option warning, key inert until installed) and appended 5 classes to test_child_delegation.py: TestAllowlistValidation, TestDelegationDepthEnforcement, TestDepthCalculation, TestChildLaunchIsolation, TestTerminalTitleFormatting (12 tests, all green in ~1.2s under T1 env). Equivalence-map docstring updated; `open_terminal` (vs second-brain `open_terminal_tab`) divergence documented as justified deviation. FR-008 grep clean. monkeypatch.setattr seam used for cache isolation; AGENT_SENDKEY_DELAY patched to 0 to keep _launch_agent under the 5s ceiling.
- 2026-05-01 — sp2.3 Done — Appended TestPreambleAntiInline (3), TestPromptBuilder (6 parametrized), TestMixedTransportPreamble (3), TestDispatchModeValidator (3) to test_child_delegation.py. All 15 green in ~1s. SC-004 invariant pinned via word-boundary regex (`\b<name>\b`) on names deliberately chosen to be non-overlapping (`cast-test-http-only` vs `cast-test-subagent-only`); anti-inline phrase asserted to appear EXACTLY once in mixed-transport prompts. Silent-fallback design pin: `dispatch_mode="subagnet"` (typo) → coerced to "http" while sibling fields (allowed_delegations, model, trust_level) round-trip unchanged — distinguishes the str+validator shape from a strict Literal annotation. Equivalence-map TODO sp2.3 markers replaced (TestNoInlineEnforcement→TestPreambleAntiInline, TestDelegationInstructionInPrompt + TestInteractivePromptBlock → TestPromptBuilder, TestMixedTransportPreambleHarness→TestMixedTransportPreamble) with verbatim-phrase + block-header anchor notes. _build_agent_prompt unchanged; tests use the agent_service.load_agent_config public seam to drive `_partition_delegations_by_mode` deterministically. Module total now 32 passed / 2 failed (the 2 reds remain sp2.2's intentional cleanup-contract drift candidates). FR-008 grep clean.
- 2026-05-01 — sp2.2 Done — Appended TestDelegationContextFile (2), TestResultSummary (3), TestFinalizeCleanup (2 RED) to test_child_delegation.py. Module wall-clock 1.74s, 17/19 green. The two reds are intentional Gate B / sp4b cleanup-contract-drift candidates: `_finalize_run` (sync, line ~1702) cleans none of {.delegation, .prompt, .continue}; `_finalize_run_from_monitor` (async, line ~2520) cleans .delegation+.prompt but NOT .continue — `.output.json` is correctly retained on both paths. Write-before-return assertion uses `logger.info` downstream-symbol monkeypatch (not a hook injected into trigger_agent). Missing-summary case routes through AgentOutput validation failure → output_data=None → result_summary=None branch. FR-008 grep clean.
- 2026-05-01 — sp2.4 Done — Appended TestContinueAgentRun (3 tests, security-relevant method FIRST) to test_child_delegation.py. US1.S10 guardrails: tmux.send_keys payload contains `Read <path>` and the run-scoped `.agent-<run_id>.continue` file path but NEVER the message body (asserted via `EVIL_INJECTION_TOKEN_8a3f` sentinel — terminal-injection contract); message bytes round-trip verbatim into the .continue file; missing tmux session raises `ValueError("...no longer exists...")`. `continue_agent_run` is async — driven via `asyncio.run`; mock target is `agent_service._get_tmux` → `MagicMock` with `session_exists` toggled per case. 3/3 green in ~1.1s under T1 env; module total 35 passed / 2 failed (the 2 reds remain sp2.2's intentional cleanup-contract drift, unchanged). Equivalence-map TODO sp2.4 marker replaced. FR-008 grep clean.
- 2026-05-01 — sp3.1 Done — Cross-checked 11 existing precondition tests in cast-server/tests/test_dispatch_precondition.py under integration env vars (CAST_DISABLE_SERVER=1, CAST_DELEGATION_BACKOFF_OVERRIDE=10ms,20ms,50ms, CAST_DELEGATION_IDLE_TIMEOUT_SECONDS=4): 11 passed in 2.83s, no regressions. Appended TestExternalProjectDirPrecondition (2 methods) to test_child_delegation.py covering ONLY genuinely-new cases — test_depth4_dispatch_returns_422_before_row_create pins US2.S7's row-count-invariance contract (the existing TestDelegationDepthEnforcement asserted only the ValueError; this method ADDS the agent_runs-row-count assertion proving the depth check fires BEFORE create_agent_run); test_invoke_route_does_not_422 pins the `/invoke` carve-out (cast-delegation-contract.collab.md §`/invoke` Carve-Out — invoke_agent at agent_service.py:1940 intentionally skips _validate_dispatch_preconditions). FR-008 grep clean (no requests/httpx/urllib). Equivalence-map docstring expanded with verbatim citations of all 9 covered-elsewhere cases plus rationale for the 2 new methods. 2/2 new pass in 0.96s; module total 37 passed / 2 failed (the 2 reds remain sp2.2's intentional cleanup-contract drift, unchanged). Combined with test_dispatch_precondition.py: 48 passed / 2 failed in 4.37s under T1 env. No new red surfaced under env-var overrides → no Gate B extension needed.
- 2026-05-01 — sp3.2 Done — Created cast-server/tests/fixtures/next_steps.schema.json (US14 typed-shape: items require {command, rationale, artifact_anchor} + additionalProperties=false) and cast-server/tests/fixtures/__init__.py. Appended TestOutputJsonContractV2 (3 methods / 5 cases) to test_child_delegation.py. Validator-pin (test_next_steps_bare_string_fails_schema, US2.S5) GREEN: bare-string entries raise jsonschema.ValidationError; sanity check on a typed entry passes — proves the schema is not a pathological always-fail. Two methods INTENTIONAL RED per US2 (sp4c candidates, NO xfail per plan §sp3.2.4): test_non_terminal_status_treated_as_malformed (parametrized over [pending, running, idle]) — root cause: AgentOutput.status (cast_server/models/agent_output.py:23) is typed `str` (not `Literal[...]`), so `_finalize_run`/`_finalize_run_from_monitor` propagate the non-terminal value verbatim into agent_runs.status without raising the parse-error contract; test_untagged_open_questions_flagged — root cause: no production validator for human_action_items tag prefixes exists yet (grep -r 'EXTERNAL\\|USER-DEFERRED' cast_server/cast_server/ returns zero matches at sp3.2 authoring time). Module total: 38 passed / 6 failed in 3.29s under T1 env (CAST_DISABLE_SERVER=1, CAST_DELEGATION_BACKOFF_OVERRIDE=10ms,20ms,50ms, CAST_DELEGATION_IDLE_TIMEOUT_SECONDS=4): 2 cleanup-drift reds from sp2.2 unchanged + 4 new sp3.2 reds. Equivalence-map docstring expanded with concrete method list, EXPECTED-RED rationale, and sp4c hand-off notes. FR-008 grep clean (no requests/httpx/urllib imports). Schema fixture is test-only per resolved open question; promotion to cast_server/contracts/ deferred to sp4d. jsonschema==4.26.0 already in env via uv.
- 2026-05-01 — sp3.3 Done — Appended TestMtimeHeartbeatRoundTrip (1 test) to test_child_delegation.py covering US2.S1 (≤1 polling-tick observation of child mtime). The diecast polling primitive is `_monitor_loop` (agent_service.py:2347) invoking `_check_all_agents` (line 2360) once per `AGENT_MONITOR_INTERVAL` via `await asyncio.sleep` (line 2356); the test semantically equates one `_check_all_agents` call to one tick (the function the loop body sleeps BETWEEN), wraps it with a counting monkeypatch, drives one tick with no output (parent must NOT transition), writes `output.json` (the write stamps mtime=now), and drives a single follow-up tick asserting parent reaches `completed`/`failed` within ≤1 tick. Mock targets: `_get_tmux` → MagicMock (idle pane + session_exists=True); `detect_agent_state` → AgentState.IDLE (so `_handle_state_transition` at line 2419-2422 checks `output_file.exists()`); `load_agent_config` → default AgentConfig; `_cleanup_parent_session` → async no-op. Test PASSES at first try → **Gate B Option C signal** per plan §Execution Notes — there is no parent-stall symptom in the basic file-canonical detection path; sp4a may have nothing to convert here, so user must decide whether to (a) accept Option C and remove sp4a from Phase 4 or (b) tighten the test (e.g. cover an mtime-update on an already-existing file or a race between monitor tick and finalize) before accepting it as green. NO xfail per US2 — the test IS the contract. Equivalence-map docstring expanded with polling-primitive citation + Gate B Option C surface note. Module total: 39 passed / 6 failed in 3.10s under T1 env (CAST_DISABLE_SERVER=1, CAST_DELEGATION_BACKOFF_OVERRIDE=10ms,20ms,50ms, CAST_DELEGATION_IDLE_TIMEOUT_SECONDS=4) — the 6 reds are the unchanged sp2.2 cleanup-drift pair + sp3.2 (3 non-terminal-status + 1 untagged-open-questions); no regressions. FR-008 grep clean (no requests/httpx/urllib).
- 2026-05-01 — sp6.1 Done — Appended TestSubagentOnlyPreamble (3 tests) to cast-server/tests/integration/test_child_delegation.py covering US4.S2 / FR-005. Phrases pinned verbatim from `_subagent_dispatch_rules` (agent_service.py:1332-1352), `_http_dispatch_rules` (1314-1329), `_quick_reference_curl` (1355-1391): subagent block header `Subagent-dispatched delegations:` + named target `cast-test-child-worker-subagent` present; HTTP block header `HTTP-dispatched delegations:` AND quick-reference anchors `Quick reference (full patterns in the skill):` / `CHILD_RUN_ID=$(curl` ABSENT (note: plain `curl` would false-positive on the API-routes `Health check: curl ...` line emitted by the preamble unconditionally — pinned the dispatch-curl substring instead, documented in class docstring); structured-return contract `Never summarize the subagent's output. Return its verdict/report structurally (pass-through).` pinned verbatim. Universal anti-inline (US1.S7 invariant) verified to fire on subagent-only allowlist. Used `_build_prompt_with_modes` helper from sp2.3 with `modes={SUBAGENT_NAME: "subagent"}`. 3/3 green in 1.52s under T1 env. Module total: 42 passed / 6 failed (the 6 reds are unchanged sp2.2 cleanup-drift + sp3.2 output-json-v2 candidates). Equivalence-map docstring entry expanded from one-line stub to method-by-method citation. FR-008 grep clean.
- 2026-05-01 — sp2.5 Done — Full T1 module under spec env vars (`CAST_DISABLE_SERVER=1`, `CAST_DELEGATION_BACKOFF_OVERRIDE=10ms,20ms,50ms`, `CAST_DELEGATION_IDLE_TIMEOUT_SECONDS=4`): 3.5s real / 1.81s pytest, well inside the 30s wall-clock budget — no tightening required. 35 passed / 2 failed. Equivalence-map docstring TODO/TBD grep returns empty; zero xfail markers in module. Gate B input enumerated below. Slowest cases: 0.47s setup `TestAllowlistValidation::test_allowed_delegation_succeeds` (only outlier; absorbs first-import cost), then 0.04s tail across `TestChildLaunchIsolation` / `TestTerminalTitleFormatting` / `TestContinueAgentRun` setup. Note: `pytest-timeout` plugin still not installed → unknown-config-option warning persists; the per-test 5s ceiling is moot today because every test is sub-second, but the plugin should be added before sp4 work to keep regression detection sharp.
- 2026-05-01 — sp5.2 Done — Appended `test_delegation_denied` (US3.S2) to cast-server/tests/e2e/test_tier_delegation.py. Asserts: parent reaches terminal status (graceful — relaxed from plan's literal `"completed"` to `_TERMINAL_STATUSES` membership because the sp1.2 fixture envelope writes `status: "failed"` and sp3.2's RED test pins that AgentOutput.status propagates verbatim; the byte-stable substring is the determinism contract per Review #5, the terminal-status is the graceful-handling contract); zero child rows linked to parent_run_id (denial fires before create_agent_run); `result_summary` contains `"422"` OR `"denied"` (pinned to literal envelope `"denied (422)"`). Verified denied fixture's `denied (422)` substring unchanged at cast-test-delegation-denied.md:74. Reused sp5.1 helpers (`_curl_post`, `_wait_for_run_terminal`, `_children_of`). AST parse OK; pytest --collect-only collects 2 tests in 0.46s under e2e marker. FR-008 grep clean. Plan/fixture conflict flagged for user — resolution via fixture update (rewrite envelope to `"completed"` while keeping `denied (422)` substring) OR plan/spec update (accept either terminal status). Sub-phase implements the latter pre-emptively to avoid a guaranteed-red test on a real host.
- 2026-05-01 — sp5.3 Done — Appended `test_mid_flight_session_isolation` (US3.S3) to cast-server/tests/e2e/test_tier_delegation.py. Added module-level helper `_list_tmux_sessions` (returns [] on rc != 0). 60s polling loop (0.5s cadence) watches for both `agent-<parent_run_id>` and `agent-<child_run_id>` to be live concurrently; if race lost, `pytest.skip("child too fast to observe mid-flight")` — skip string verbatim from second-brain. Both-alive branch asserts (a) parent and child session names differ (isolation invariant) and (b) parent session has exactly 1 pane (`tmux list-panes -t agent-<parent_run_id> -F #{pane_index}`). NO slow-child knob introduced (plan §Out of Scope explicit). Reused sp5.1 helpers (`_curl_post`, `_children_of`). AST parse OK; pytest --collect-only collects 3 e2e tests in 0.52s. FR-008 grep clean.
- 2026-05-01 — sp5.4 Done — Authored `.github/workflows/cast-delegation-e2e-nightly.yml` per Gate C Option A. `runs-on: [self-hosted, ptyxis]`; cron `37 6 * * *` UTC (slotted between existing nightlies at 06:17 and 06:47 to avoid log collisions); `workflow_dispatch: {}` enabled; `timeout-minutes: 30`. Install pattern mirrors `setup-correctness.yml` / `anonymization-lint.yml`: idempotent `uv` bootstrap (skipped if the self-hosted host already has it) → `uv sync --group dev` → `uv run pytest cast-server/tests/e2e/test_tier_delegation.py -m e2e -v`. Prereq-version step surfaces missing `ptyxis` as a WARNING so a mislabeled runner is loud. **Production cadence preserved** — NO T1 env-var overrides (real polling/backoff exercised). **Port alignment via env var** — `CAST_E2E_PORT=8005` set in the workflow to pin the e2e spawn fixture (default 8765) to the port the four delegation fixture .md prompts curl, resolving the sp5.1 hand-off without modifying test code (plan §"Out of scope: Modify the test code" preserved). `actionlint` not available in the local env; PyYAML parse OK. sp7.1 accumulates ≥3 nightly runs for SC-002.
- 2026-05-01 — sp5.1 Done — Created cast-server/tests/e2e/{__init__.py, conftest.py, test_tier_delegation.py, pytest.ini} establishing the T2 live-HTTP suite scaffold. Session-scoped `cast_server_process` spawns `bin/cast-server` on `CAST_E2E_PORT` (default 8765, host 127.0.0.1) with `CAST_TEST_AGENTS_DIR=cast-server/tests/integration/agents` exported into the subprocess env (sp1.3 hand-off honored), polls `GET /api/agents/runs?status=running` as the liveness probe up to 30s (cast-server has no `/healthz`; `/api/health` shells out to bin/cast-doctor — too slow), fails fast if the port is occupied or the process exits during startup, and SIGTERMs the new process group on session-end (SIGKILL after 10s grace). Per-test `reset_e2e_goal_dir` idempotently creates the `child-delegation-e2e` goal via `goal_service.create_goal` + `goal_service.update_config(external_project_dir=...)` to satisfy US2.S7 dispatch precondition, purges runtime files (`.agent-*`, `.delegation-*`, `*.prompt`, `*.continue`, `*.output.json[.tmp]`) while preserving `goal.yaml` + `requirements.human.md`, and DELETEs all `agent_runs` rows scoped to the slug (pre- and post-test for `pytest -x` rerun safety). `test_parent_delegator_happy_path` (US3.S1) skeleton uses bash-curl helpers (`_curl_post` / `_curl_get`) — FR-008 grep clean, no requests/httpx/urllib imports — polls DB row terminal state via `agent_service.get_agent_run` (production cadence: 300s timeout, 2s poll interval), asserts parent.status=completed, asserts ≥1 child via `agent_service.get_runs_for_goal` filtered by parent_run_id with non-empty result_summary, asserts child tmux session `agent-<run_id>` is torn down post-finalize via `tmux has-session`. `@pytest.mark.e2e` registered in `cast-server/tests/e2e/pytest.ini` (no warning); `@pytest.mark.timeout(360)` decorator + `timeout = 360` ini key emit the same PytestUnknownMark/UnknownConfigOption warnings as the integration suite until `pytest-timeout` is installed (sp2.5 posture preserved). pytest --collect-only collected 1 test in 0.49s; ast parse OK. **Open hand-off for sp5.4 / pre-CI:** the four delegation fixture .md prompts under `cast-server/tests/integration/agents/` hard-code `http://localhost:8005` for the child-trigger curl; the spawn fixture uses port 8765 to avoid colliding with developer cast-server. sp5.4 must decide whether to (a) template fixture prompts with a `<cast_server_url>` placeholder or (b) pin the e2e spawn port to 8005 with a port-occupied precondition — until then `cast_server_base_url` and the fixture-prompt port disagree and a real run will dispatch to the wrong port. Skeleton is ready for sp5.2 (delegation_denied) and sp5.3 (mid-flight session isolation) to append additional `@pytest.mark.e2e` cases.
