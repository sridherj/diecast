Your run ID: run_20260501_074719_70611d
Goal slug: runs-threaded-tree

Diecast API routes (http://localhost:8005):
  POST /api/agents/{name}/trigger        — dispatch a child agent
  GET  /api/agents/jobs/{run_id}          — get run details/status
  POST /api/agents/jobs/{run_id}/recheck  — recheck a completed/failed run
  POST /api/agents/runs/{run_id}/continue — send message to idle agent
  POST /api/agents/runs/{run_id}/cancel   — cancel an active run
  POST /api/agents/runs/{run_id}/fail     — mark run as failed
  POST /api/agents/runs/{run_id}/complete — mark run as completed
  DELETE /api/agents/runs/{run_id}        — delete a terminal run
  GET  /api/agents/runs                     — list all runs
  GET  /api/agents/runs?status=running      — filter runs by status
  GET  /api/agents/runs/{run_id}/children — list child runs
  POST /api/tasks/{task_id}/run-agent     — trigger agent for a task
  Health check: curl -s http://localhost:8005/api/agents/runs?status=running | head -1
Use the cast-subphase-runner agent.

Goal: runs-threaded-tree
Task: 
Expected outcome: 
Output directory: /data/workspace/diecast

Additional context: Execute sub-phase 3 (sp3_htmx_endpoint) of the runs-threaded-tree execution plan. RE-DISPATCH after a stale run was killed by an unrelated process.

Project dir: /data/workspace/diecast
Shared context: docs/execution/runs-threaded-tree/_shared_context.md
Sub-phase plan: docs/execution/runs-threaded-tree/sp3_htmx_endpoint/plan.md
Manifest: docs/execution/runs-threaded-tree/_manifest.md

Dependency: sp1 + sp2 are Done. get_runs_tree() exists; the macro + fragment + route swaps are live. fragments/run_status_cells.html already has the conditional hx-get/hx-trigger/hx-swap pointing at /api/agents/runs/{id}/status_cells — your job is to add that endpoint plus the markup-shape test.

CRITICAL: Write your terminal output.json to /data/workspace/diecast/goals/runs-threaded-tree/.agent-<YOUR_RUN_ID>.output.json — DO NOT write to .cast/ or any other goal_dir. The path is goals/runs-threaded-tree/ under the project root. The goal_slug is runs-threaded-tree, not anything else you might infer from the dashboard.

Steps:
1. Read _shared_context.md and sp3_htmx_endpoint/plan.md.
2. Update _manifest.md row 3 status to In Progress (if not already).
3. Add GET /api/agents/runs/{run_id}/status_cells to cast-server/cast_server/routes/api_agents.py. Fetch the run, compute its current ctx_class/rollups via the same path get_runs_tree uses for individual rows, render run_status_cells.html with that context. Return HTMLResponse.
4. Create cast-server/tests/test_runs_template.py with 3 markup-shape tests: (a) status cells carry hx-get/hx-trigger/hx-swap when run.status in (running, pending, rate_limited); (b) outer .run-node MUST NOT carry hx-* attrs; (c) hx-target lands on the inner cells span by id.
5. Run: uv run precommit-tests --suite unit  (must pass).
6. Update _manifest.md row 3 status to Done.
7. Write contract-v2 output.json at /data/workspace/diecast/goals/runs-threaded-tree/.agent-<RUN_ID>.output.json (atomic write). status=completed, summary, artifacts[], human_action_needed=false.
## External Project Directory
You are running inside the project directory (/data/workspace/diecast).
Goal artifacts (plans, research, playbooks) are in `.cast/`.
Code and execution artifacts go in the current working directory.
Work in /data/workspace/diecast. Write all artifacts there.

CRITICAL: NEVER inline an agent's work yourself. When a task calls for one of your allowed_delegations, you MUST dispatch to that agent — do not attempt the work yourself, summarize what the agent would produce, or paraphrase its output.
Your allowed_delegations: [cast-review-code]

HTTP-dispatched delegations: [cast-review-code]
NEVER use the Agent tool for these targets. All child agent work for the listed agents MUST go through HTTP dispatch via the delegation skill. If HTTP dispatch fails (curl error, server down), STOP and ask the user — do NOT fall back to the Agent tool.

Agent composition: If your task would benefit from another agent's capabilities,
invoke the `/cast-child-delegation` skill BEFORE dispatching any child. It covers
dispatch, polling, status checks, continue vs. new trigger, and parent-child context.

Quick reference (full patterns in the skill):

  CHILD_RUN_ID=$(curl -s -X POST http://localhost:8005/api/agents/{agent}/trigger \
    -H "Content-Type: application/json" \
    -d '{
      "goal_slug": "runs-threaded-tree",
      "parent_run_id": "run_20260501_074719_70611d",
      "delegation_context": {
        "agent_name": "{agent}",
        "instructions": "What the child should do",
        "context": {
          "goal_title": "...",
          "artifacts": ["relevant artifact paths"],
          "prior_output": "summary of your work so far"
        },
        "output": {
          "output_dir": "{output_dir}",
          "expected_artifacts": ["what you expect back"]
        }
      }
    }' | jq -r '.run_id')

  GOAL_DIR="/data/workspace/diecast/.cast"   # use this exact value — injected from your prompt preamble
  TIMEOUT=2700; ELAPSED=0
  while [ $ELAPSED -lt $TIMEOUT ]; do
    [ -f "$GOAL_DIR/.agent-$CHILD_RUN_ID.output.json" ] && break
    sleep 10; ELAPSED=$((ELAPSED + 10))
  done
  # On timeout, check: curl http://localhost:8005/api/agents/jobs/$CHILD_RUN_ID | jq '.status'

IMPORTANT — when you are completely finished (as your very last action), write this exact JSON structure to /data/workspace/diecast/.cast/.agent-run_20260501_074719_70611d.output.json:

{
    "contract_version": "2",
    "agent_name": "cast-subphase-runner",
    "task_title": "",
    "status": "completed | partial | failed",
    "summary": "One paragraph describing what you accomplished",
    "artifacts": [
        {"path": "relative/to/goal/dir/file.md", "type": "research|playbook|plan|code|data", "description": "What this file contains"}
    ],
    "errors": [],
    "next_steps": ["Suggested follow-up actions for the user"],
    "human_action_needed": false,
    "human_action_items": [],
    "started_at": "2026-05-01T07:47:20.038388+00:00",
    "completed_at": "<fill in current ISO timestamp when you write this file>"
}

Status values:
- "completed" = all requested work done successfully
- "partial" = some work done, but not everything (explain in summary)
- "failed" = could not accomplish the task (explain in errors)

Set human_action_needed to true when:
- You need human approval before changes take effect
- You found data issues that need manual correction
- You have open questions that block completion
- You completed the work but something needs human verification
Keep human_action_items specific and actionable (what exactly to do).

Artifact paths must be relative to /data/workspace/diecast/.cast.

## Known Error Patterns for This Agent

The following errors have occurred in past runs. Avoid repeating them:

- **timeout** (2x, last: 2026-05-01): Dynamic verification (re-running pytest cast-server/tests/ui/) blocked: /data/workspace/diecast/.venv is owned by root and uv cannot manage it. uv error: `failed to remove directory /data/workspace/di