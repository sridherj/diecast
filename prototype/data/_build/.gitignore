# Authoring tooling only — installed faker lives here, never committed, never loaded by the browser.
# (Root .gitignore already ignores node_modules/ globally; this is the explicit local restatement.)
node_modules/
