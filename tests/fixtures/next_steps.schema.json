{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Cast next_steps typed array",
  "description": "Typed shape for the next_steps field in .agent-run_<RUN_ID>.output.json (US14).",
  "type": "array",
  "minItems": 0,
  "maxItems": 3,
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["command", "rationale", "artifact_anchor"],
    "properties": {
      "command": {
        "type": "string",
        "minLength": 1,
        "description": "A /cast-* slash command or a shell command. Render-only — never auto-executed."
      },
      "rationale": {
        "type": "string",
        "minLength": 1,
        "description": "One-line reason grounded in the artifact/state just produced."
      },
      "artifact_anchor": {
        "type": ["string", "null"],
        "description": "File path most relevant to the suggestion, or null."
      }
    }
  }
}
