# Execution Manifest: Product Revamp Diecast — Phase 5 (Colleague Surfaces)

## How to Execute

Each sub-phase runs in a **separate Claude context**. The dependency DAG is
`5.0 → (5a ∥ 5b ∥ 5c) → 5.4` with **critical path 5.0 → 5b → 5.4**. For each sub-phase:
1. Start a new Claude session (or dispatch a `cast-subphase-runner`).
2. Tell Claude: "Read
   `docs/execution/product-revamp-diecast-phase5-colleague-surfaces/_shared_context.md`, then execute
   `docs/execution/product-revamp-diecast-phase5-colleague-surfaces/spN_name/plan.md`."
3. After completion, update the Status column below and append a Progress-Log line.

**FULL AUTONOMY (owner-approved, end-to-end through Phase 6):** no user questions, no approval gates,
no idle waits; pick the recommended option at every gate and **propagate the directive to any child
agent** (the slop-gate visual/tone checkers in 5.4).

**This phase ships CODE** — all changes land in the single file `prototype/index.html` plus the one
generator-authored ORG extension batch (owned by 5.0). **NO TESTS anywhere** — verification is manual
click-through / static observation only, with non-blocking human-eyeball carry-forwards for
rendered-pixel items (no live browser guaranteed in autonomous runs).

### Binding constraints (full text in `_shared_context.md`; the runner MUST read it first)
1. **NO TESTS** — no test files/suites/harness/CI; manual click-through only; never flag missing tests.
2. **`file://` legality** — ONE inline `index.html`; no `fetch()`, no local ES-module imports; only
   https CDN imports, classic `<script src>`, relative `<img>`; disclosure uses native `<details>`.
3. **ORG FROZEN (2a FREEZE)** — all data extensions via `generate-org.mjs` (gate re-runs, refuses on
   violation); **never hand-edit `org.js`**; additive keys only; **generator single-owned by 5.0**.
4. **Section-stability (Reconciliation F4)** — ORG sections outside the declared additions stay
   byte-identical; **CAST-452/CAST-461 (Phase 4) sections byte-identical** (parallel-phase guard).
5. **Generator serialization (Reconciliation F3)** — Phase 4.1's batch already committed `org.js`;
   5.0's batch layers additively on top.
6. **2c stage vocabulary is canonical** — the Layer-2 chain pipeline reads stage labels from ORG;
   hardcoded vocabulary is a defect.
7. **Closed 5-op vocabulary** — UI state (filters, dial, disclosures, wizard Next) = plain handlers +
   additive appState keys; `SCRIPTS.hiring` patches state directly; **no sixth op anywhere**.
8. **vt- anchors 6×1, shell-zone-wrappers only** — a duplicate name silently kills all transitions;
   the ten new routes introduce no vt- name.
9. **L3 budget: exactly one hard stop per flow** — this phase adds ZERO new L3 atoms; CAST-417's rail
   stays an unresolved stop; the dial's L2→stop promotion is a scripted illusion (ORG unmutated).
10. **Failure policy:** retry once; second failure off the critical path → log gap + continue; on the
    critical path (5.0 → 5b → 5.4) → **stop and report**.

### Binding context docs the runner MUST read first
- `docs/plan/2026-06-11-product-revamp-diecast-phase5-colleague-surfaces.md` (the plan)
- `docs/plan/product-revamp-diecast-decisions-so-far.md` (run config, NO-TESTS, Phase 4 close record)
- `docs/plan/2026-06-12-product-revamp-diecast-reconciliation.md` (F2 script-set, F3/F4 generator rules)
- `docs/plan/product-revamp-diecast-stage-models.md` (canonical stage vocabulary — compare against)

## Sub-Phase Overview

| # | Sub-phase | Directory/File | Depends On | Status | Notes |
|---|-----------|----------------|-----------|--------|-------|
| 5.0 | Shared Rails — ORG Extension, Route Skeleton & the Digest Atom | `sp5_0_shared_rails/` | — (Phases 1–4 done) | Verified | Owns the ONE generator batch (`requirements_doc`, `agents[].versions/monitoring`, `org.skills`, `dial_demo`) + new invariants; the ten route stubs; the four additive appState keys; the `DigestNotice` component. **Critical path (root); unblocks all three sub-streams.** |
| 5a | Board Arc, Decision Trail & the Autonomy Dial (US5 + US10) | `sp5a_board_arc/` | 5.0; Phase 3 (`IterationPanel`) | Verified | Real `#/board` (4 columns, working assignee filter) → `#/ticket/CAST-412` (IterationPanel log + rework 1/3 + PR link) → `#/decision/:atomId` (artifact + CAST-417 escalation branch) → `#/decisions/CAST-412` (diff-first trail) + `AutonomyDial` wiring + L2 digest strip. **Parallel with 5b/5c.** |
| 5b | Hiring Funnel, Marketplace, Agent Ops & Layer-2 (US6 + US8 + US9) | `sp5b_hiring_marketplace/` | 5.0 | Verified | `SCRIPTS.hiring`; `#/hire` 5-step wizard (assessment → federation → stack-ranked report → hire → onboard); `#/marketplace` (12 agents / 6 archetypes + scope filter); `#/agent/:slug` resume + Versions/Monitoring tabs (`RadarChart`/`Sparkline`); `#/skills/new`; `#/layer2`. **Six surfaces — widest sub-stream; greenfield concentration. Critical path.** |
| 5c | Requirements-Doc Loop (US7) | `sp5c_reqs_doc/` | 5.0 | Verified | `#/reqs/CAST-412`: classification pill, L1/L2/L3 typographic disclosure, the one PM-framed comment thread, v1→v2 delta review, `DigestNotice` write-back, decision chip on the REST-over-GraphQL element. **Parallel with 5a/5b.** |
| 5.4 | Stitch, Cross-Links, Slop Gate & Drift Sweep | `sp5_4_stitch_gates/` | 5a, 5b, 5c | Verified | Cross-link audit (every avatar → resume; the loop-back; ID-match spot-checks); slop gate on six surfaces (delegate `/cast-preso-check-visual` + `/cast-preso-check-tone`); extended drift grep; script/state/reload sanity. **No new design — enforces the earlier ones. Critical path (terminal).** |

Status: Not Started → In Progress → Done → Verified → Skipped

> **No decision gates in this phase.** Full-autonomy mode resolves all judgment calls; there are no
> human-pause points and no `gate_*` files. Plan review and reconciliation passes are SKIPPED per the
> owner-approved run config (Phase 1/2a/2b/2c/3/4 precedent) — this split adds **no** review/recon
> sub-phases.

## Dependency Graph

```
            Sub-phase 5.0  (shared rails: ORG batch · routes · DigestNotice)
                  │
      ┌───────────┼───────────────┐
      ▼           ▼               ▼
 Sub-phase 5a  Sub-phase 5b   Sub-phase 5c
 (board arc ·  (hiring funnel ·  (reqs-doc
  trail · dial)  marketplace ·     loop)
                 ops · Layer-2)
      └───────────┼───────────────┘
                  ▼
            Sub-phase 5.4  (stitch · slop gate · drift sweep)

   Critical path: 5.0 ──► 5b ──► 5.4   (5b = widest sub-stream + greenfield concentration)
   5a ∥ 5b ∥ 5c are fully parallel after 5.0 — separate routes, separate CSS prefixes,
   shared code only through the 2b kit + 5.0's DigestNotice/RadarChart/Sparkline.
```

## Execution Order

### Sequential Group 0 (the gate — must finish before any sub-stream)
- **5.0** `sp5_0_shared_rails` — the single generator batch (owns `generate-org.mjs`), the ten route
  stubs, the additive appState keys, the `DigestNotice` component.

### Parallel Group 1 (run concurrently after 5.0 — `--max-batch-size 3`, exactly three members)
- **5a** `sp5a_board_arc` — board → ticket → decision → escalation arc + trail + dial wiring
- **5b** `sp5b_hiring_marketplace` — hiring wizard + marketplace + agent ops + Layer-2 (critical path)
- **5c** `sp5c_reqs_doc` — the US7 requirements-doc loop

> This parallel group has **exactly 3 members**, so it fits one batch at `--max-batch-size 3` — no
> sub-batching needed.

### Sequential Group 2 (after 5a + 5b + 5c)
- **5.4** `sp5_4_stitch_gates` — the stitch/cross-link/slop-gate/drift pass must see all three streams.

> **File-collision honesty note (mirrors the Phase 3/4 splits' serial override):** the plan calls
> 5a/5b/5c "fully parallel" via disjoint routes + CSS prefixes, and this manifest models them as one
> parallel batch per the mandated DAG. But all three edit the **same single file**
> `prototype/index.html`, and there is **no merge mechanism** between two independent runner agents.
> If 5a/5b/5c are dispatched concurrently, **serialize their `index.html` writes** (each appends its
> own disjoint banner section; land them in sequence) or run them in one session. The generator is
> single-owned by 5.0 regardless, so `org.js` is never written concurrently. The logical parallelism
> (disjoint zones, independent data slices) is real; the physical serialization is a single-file
> artifact constraint, not a deviation from the DAG.

## What This Phase Delivers (high-level plan verification, restated)

- The **board arc** reads as **four frames of one story** with consistent chrome and canonical
  vocabulary: `#/board` (humans + agents in one assignee stack, working filter) → `#/ticket/CAST-412`
  (maker-checker activity log with inline M04/S03/R02 violations + rework 1/3 + PR link) →
  `#/decision/:atomId` (full decision artifact; CAST-417 branches to the escalation rail) →
  `#/decisions/CAST-412` (diff-first cross-phase trail, GraphQL→REST struck through).
- The **autonomy dial toggle visibly promotes an L2 decision into an L3-style stop** (SC-007's
  autonomy-gated moment, shown live; ORG unmutated, reload resets).
- **"Hire. Don't install."** is a clickable flow: chat asks for an rbac-agent → 5-screen wizard
  (assessment → federation → stack-ranked eval report with radar + pros/cons + deep links to produced
  work → hire maker+checker together → onboard); the marketplace shows 12 agents / 6 archetypes;
  every avatar opens a full resume with Versions + Monitoring ops tabs; Layer-2 is enumerable.
- The **US7 requirements-doc loop**: `#/reqs/CAST-412` renders classification pill, L1/L2/L3
  typographic disclosure, the one PM-framed comment thread, v1→v2 delta review, and the write-back
  notice — all from `goals['CAST-412'].requirements_doc`.
- **Cross-surface integrity:** every avatar everywhere opens the right resume; both slop-gate checkers
  pass on all six gated surfaces; the drift grep returns hits only from `org.js`.

## Progress Log

<!-- Each runner appends a one-line dated entry after completing its sub-phase. -->
- **2026-06-12 · 5.0 Shared Rails — Done.** ONE generator batch landed in `generate-org.mjs` (re-run, byte-identical across two runs; gate green on all old + 6 new invariants): `goals['CAST-412'].requirements_doc` (6 elements w/ L1/L2/L3 spread, one PM comment, v1→v2 delta + planning write-back), `agents[].monitoring` (12-pt trend + cost/latency/recent_runs; full on crud-orchestrator, thin elsewhere; credibility stays single-source on `stats`), `org.skills` (3 company + private `cast-export-csv`), and the `dial_demo:true` marker on the one L2 atom (DEC-CAST-412-06). Existing `agents[].versions` reused to satisfy the new ≥1-version invariant (FREEZE: never mutated). `git diff org.js` additive-only (537 ins / 2 comma-deltas); CAST-452/CAST-461 goal slices **byte-identical** (verified + a Rule-20 parallel-phase guard added). `index.html`: nine new route stubs via one `RouteStub` (+ `#/board` keeps `BoardStub`) → all ten routes resolve to labeled stubs in the standard shell; nav rail gains Board/Hire/Layer-2; four additive appState keys (`boardFilter`, `hiring`, `autonomyLevel`, `reqsDoc`); `DigestNotice` (one definition, pure props, native-`<details>` rows) fixture-rendered on `#/kit`. `node --check` clean; vt- anchors unchanged (6×1); no new op; no tests. **Human-eyeball carry-forward (non-blocking, no live browser in this run):** open `prototype/index.html`, visit the ten routes + `#/kit`, confirm labeled stubs render with no console errors and the DigestNotice rows disclose.
- **2026-06-12 · 5b Hiring / Marketplace / Agent Ops / Layer-2 — Done.** Replaced the five 5.0 stubs with real views in the single `prototype/index.html` (5a/5c stubs untouched; `#/board`/`#/ticket`/`#/decision*`/`#/reqs` renderers never touched). **`SCRIPTS.hiring`** (6 Guide-voiced beats, all tokens from `ORG.hiring`) drives `appState.hiring.step`; a `__hire` chat bucket + `chatKey()` make the rail a first-class driver without polluting per-goal threads — the wizard's own Next/Back buttons set the same state, so both paths sync by construction (no sixth op; patches mutate state directly). **`#/hire`** 5-step wizard: (1) 5-dim assessment matrix from `ORG.hiring.dimensions`, (2) federation grid — 4 scored + 2 in-flight pills, static (no timers), (3) stack-ranked leaderboard from `report.ranked` with per-row expand → `RadarChart` + numeric score + pros/cons + produced-work `<details>` stubs + head-to-head top-two compare toggle (eval-report-card shape, not a pricing grid), (4) maker+checker hired together in one in-card pairing, (5) teammate-onboarding checklist linking to the dial legend (`#/decisions/CAST-412`). **`#/marketplace`**: 12 agents / 6 archetype facet chips + open/internal scope filter (facets live in the hash — `#/marketplace/<arch>/<scope>` — no new appState key), card-density `ColleagueCard` via `toCardAgent`, each card a link to its résumé, `org.skills` in a slim section with scope badges, "Hire for a capability →" CTA. **`#/agent/:slug`**: résumé (role / I/O / autonomy / paired-checker link / benchmark `RadarChart` / sample output / track record) + **Versions** (SHA-pinned) + **Monitoring** (`Sparkline` trend, cost/latency p50, last-N runs, "replay →" landing on Phase 3's execution drill-in via a `data-replay` handler) tabs in the hash; crud-orchestrator deep, other 11 thin from the same component; unknown slug → muted not-found strip. **`#/skills/new`**: two frames — terminal one-liner reusing the Phase-4 `parity-term` ink-dark treatment + private/company choice → confirmation card badged in the catalogue with a display-only promote affordance. **`#/layer2`**: one page, three scroll-nav sections — 12-contract catalogue (8 chain-aligned + 4 cross-cutting, I/O signatures), the 8-agent chain reusing `StageSpine`'s pipeline shape (labels read from `ORG.layer2.chain`, CAST-412 position = first active node, derived from status — no hardcoded stage vocabulary), and the 6-project portfolio. **`RadarChart`/`Sparkline`** = hand-authored data-driven inline SVG (real `<text>`, `<title>`/`<desc>`, existing tokens only — never rasters). cred digits single-sourced from `agent.stats` (crud card reads **99.9% compliant · 2 loops · 505 runs**, identical to its résumé). `node --check` clean; vt- anchors unchanged (6×1); closed 5-op set intact; no new op; `org.js` never touched; NO TESTS. **Taste calls (documented):** layer2 sections use plain scroll-nav buttons not `#id` anchors (which collide with the hash router); head-to-head = side-by-side columns; scope derived as makers/checkers=open Diecast vs orchestration archetypes=internal; agent benchmark-radar axes are deterministic projections of real `agent.stats`/health/autonomy numbers. **Observation for 5.4 (not a 5b defect):** the 5 orchestration-archetype agents are modeled `kind:maker`/`pairedWith:null` in ORG, so `ColleagueCard` honestly renders its unpaired-maker ⚠ on their marketplace cards — the kit's zero-silent-failure grammar, faithful to the data. **Human-eyeball carry-forward (non-blocking, no live browser in this run):** open `prototype/index.html`, walk `#/hire` both ways (chat Next ▸ AND wizard buttons) through all 5 steps incl. a row expand + a produced-work link + head-to-head; count 12 cards / 6 archetypes on `#/marketplace` and exercise the scope filter; open a résumé's Versions + Monitoring tabs and the "replay →" link; complete `#/skills/new` in two frames; scroll the three `#/layer2` sections and confirm the chain highlights; eyeball radar/sparkline legibility + the slop-gate on all six surfaces.
- **2026-06-12 · 5a Board Arc, Decision Trail & Autonomy Dial — Done.** Replaced the four 5.0 stubs (`#/board`, `#/ticket`, `#/decision`, `#/decisions`) with real renderers in the single `prototype/index.html`; 5b's five routes + 5c's `#/reqs` untouched; `org.js` never touched. New `board-*`/`ticket-*`/`dec-*` CSS section + four view fns read as **four frames of one story**. **`#/board`**: 4 columns from `ORG.board`, humans + agents in ONE assignee stack (each card embeds a line-density `ColleagueCard` + ticket-level in-flight pill, distinguished only by 2b avatar grammar — no Automations lane); the verbatim framing line "Publishes INTO your PM tool…"; an `@you · 1` escalation inbox linking to the CAST-417 decision frame (DEC-CAST-412-04, derived); the **Any/Human/Agent/Checker** filter chip row drives `appState.boardFilter` via plain `setBoardFilter` handlers (counts 9/1/8/1 — each hides the wrong cards), NOT an op. **`#/ticket/CAST-412`**: header = title + maker-checker paired lockup + 3-segment rework meter (1/3); body = **Phase-3 `IterationPanel` reused verbatim** (M04/S03/R02 finding checklist, named exits, PR #2341 review thread); a decision-chip rail renders the 6 ticket atoms as 6B callouts with the execution L2 (DEC-CAST-412-03) carrying the **"next › decision →"** link; footer PR #-link routes the diff behind the **execution drill-in** via the existing `data-replay` handler. **`#/decision/:atomId`**: the 6C full record — every atom field verbatim (id, reversibility, status, phase, rationale, options, consequences, revisit_if, what_i_want/tried, originating, timestamp, supersedes/superseded_by links, spike_ref, influenced); **BRANCH** — an L3 atom with an escalation pack (CAST-417) also renders the single `EscalationRail` (3 ranked options, nothing pre-selected, options stay inert — an **unresolved stop**; reuses the Phase-3 `atomToEscalation` mapping verbatim); "escalated to me →" sets `boardFilter='human'` then navigates to `#/board` (filter round-trips). **`#/decisions/CAST-412`**: diff-first trail (one `Decision` row per atom, chronological) with the **GraphQL→REST** superseded pair (DEC-CAST-412-01 → 02) struck-through + "superseded by →"; phase/actor/L-level filter chips live in the **hash** (`#/decisions/<id>/<phase>/<actor>/<lvl>`, marketplace precedent — no new appState key); rows link to `#/decision/:atomId`. **`AutonomyDial` wiring** at the trail header: added a backward-compatible `onPick` prop (the `#/kit` static gallery passes none → stays static); toggling sets `appState.autonomyLevel` + re-renders. **Under Conservative** the `dial_demo` L2 atom (DEC-CAST-412-06) LEAVES the digest (2→1) and the trail flow and renders as a **pinned stop-and-confirm card** at the top (rasp escalation treatment, re-tinted L2 badge, "now requires your OK", conditional-voice copy, inert confirm button) — a **scripted illusion**: no receipt written, ORG never mutated, reload resets to Balanced. **L2 digest strip** via the shared `DigestNotice` (glyph ⚖, "2 decisions made while you were away", rows disclose to the 6B callout). Two surgical kit-link updates: `DecisionCallout`'s "open full record →" and `DecisionRow`'s superseded link now target the real `#/decision/:atomId` frame (the latter de-anchored to a span so trail rows wrap cleanly). `node --check` clean; vt- anchors unchanged (6×1); closed 5-op set intact (filters/dial = plain handlers + hash facets, no sixth op); a `/tmp` logic harness (never committed) confirmed every data flow against the real `org.js` (board filter counts, ESC_ATOM=DEC-CAST-412-04, 6 trail atoms, dial_demo=DEC-CAST-412-06, digest 2/1, GraphQL→REST pair, next-atom, iteration M04/S03/R02 · rework 1/3 · PR #2341, trust 99.4%·312); NO TESTS. **Human-eyeball carry-forward (non-blocking, no live browser in this run):** open `prototype/index.html`, click the four-frame arc end-to-end (board card CAST-412 → ticket → "next › decision" → artifact → from the trail open CAST-417 → escalation rail with nothing pre-selected → "escalated to me" loops back to a filtered board); click all four board filter chips; on the trail flip the dial Balanced↔Conservative and watch the dial_demo L2 promote to a pinned stop (reload resets); confirm the GraphQL→REST struck pair, the digest disclosure, and a muted not-found strip for an unknown `:atomId`/ticket id.
- **2026-06-12 · 5c Requirements-Doc Loop (US7) — Done.** Replaced the last 5.0 stub: `#/reqs` → `ReqsDocView`, reading **entirely** from `goals['CAST-412'].requirements_doc` (new `reqs-*` CSS section; 5a/5b routes untouched; `org.js` never touched). **Header** = workflow-classification pill (`feature`, mono) + version chip (`v2`) + a native-`<details>` **version-history popover** (v1/v2 dates + the v2 split-summary) + the goal title. **Body** = the element hierarchy as a recursive `ReqElement` over native `<details>` where **hierarchy depth is purely TYPOGRAPHIC** (`reqs-lvl1/2/3` = size·weight·indent + a guide-rail) and **carries NO L-badge** — the L1/L2/L3 collision rule enforced in CSS: the only L1/L2/L3 badges on this screen ride the **decision chips** (`DecisionCallout`/`LBadge`), always ⚖-prefixed. L1 (req-01) row always visible → its L2s one disclosure in (open by default) → each L2's L3s nested one deeper (collapsed by default, so the auto-expand edge is observable). **The one PM-framed moment, prototype-wide:** req-02 carries the single comment affordance → `appState.reqsDoc.openComment` opens a side-anchored thread (`@priya` + `@you`, 2 messages); each author's circle `Avatar` + **role tag come from `ORG.humans`** (never hardcoded — `Priya Kannan · PM` highlighted), with an open↔resolved display toggle (additive `reqsDoc.commentResolved`, reload clears). **Delta review:** the write-back **`DigestNotice`** (the SAME 5.0 component, `↺` voice, naming origin phase `planning`) sits up top; its CTA drives `appState.reqsDoc.deltaView`, rendering a **diff-first** change list in the decision-trail row grammar (diff LEADS, then the `req-NN ▸` jump button + origin tag) anchored to the 3 affected elements. **Decision chip** on the REST-over-GraphQL element (req-03) = `DEC-CAST-412-02` (accepted; the retired GraphQL `…-01` is filtered out as superseded) — opens the same 6B popover and **its id matches the `#/decisions/CAST-412` trail row** (cross-stream ID-match); chips also ride req-02/04/05/06 from their `decision_refs`, each ⚖-badged. **Auto-expand edge handled:** a node's child disclosure force-opens when any descendant is an active anchor, so turning on the delta (anchors req-04/req-05, nested in req-02's collapsed L3 disclosure) pops the chain open + highlights the rows — a highlight inside a closed `<details>` is never left invisible. **Entry link:** the CAST-412 canvas requirements stage (`DocSurface`, first feature doc-step only — gated on `goal.requirements_doc`) gains an in-flow `<a href="#/reqs/CAST-412">` (no new op). `node --check` clean; **exactly one `DigestNotice` definition** in source; vt- anchors unchanged (6×1 `view-transition-name` set); closed 5-op set intact (toggles = plain handlers + additive appState, no sixth op); a `/tmp` logic harness (never committed) confirmed against the real `org.js` (roots=[req-01]; all delta/comment/writeback anchors resolve; deltaView force-opens req-02; REST chip=DEC-CAST-412-02/accepted on the trail; exactly one pm author=Priya Kannan); NO TESTS. **Human-eyeball carry-forward (non-blocking, no live browser in this run):** open `prototype/index.html` → `#/reqs/CAST-412`, confirm the pill + v2 chip + history popover; collapse/expand an L2 and an L3; click req-02's 💬 and watch the PM-tagged thread anchor beside it + flip open↔resolved; click the write-back CTA and confirm the delta rows highlight + the collapsed L3 chain auto-expands; open the req-03 REST chip and eyeball its id against the `#/decisions/CAST-412` trail row; follow the canvas requirements-stage entry link in-flow.
- **2026-06-12 · 5.4 Stitch, Cross-Links, Slop Gate & Drift Sweep — Done (terminal critical-path node; phase gate PASSED).** No new design — this pass ENFORCES 5a/5b/5c. **(1) Cross-link audit:** found + fixed the only two inert-avatar gaps — the `#/ticket` header maker/checker lockup (`ticket-pair-who`) and the `IterationPanel` maker/checker `ColleagueCard`s (the ticket activity log, also the goal-canvas exec drill-in) were avatars-that-went-nowhere; both now wrap in `<a href="#/agent/:slug">` (link-reset CSS + `:hover` affordance), resolving to real agents (crud-orchestrator / crud-compliance-checker). Every other avatar already lands: marketplace cards (`mkt-card-link`), résumé paired-checker (`ops-pairlink`) + ops tabs, hire-step-4 checker (security-checker) — all RESOLVE. **Deliberate, data-grounded non-links (NOT defects):** board cards link to `#/ticket/:id` (the card's primary affordance; the maker/checker résumé is one hop in via the now-linked ticket header — nested `<a>` is illegal); hiring-report candidate avatars are **prospective candidates absent from `ORG.agents`** (rbac-architect et al.) — the expand-eval (radar + pros/cons + produced work) IS their dossier, so `#/agent/<candidate>` would be a dead not-found and is correctly omitted; the trail "who" is plain actor text (no `Avatar`), the row links to `#/decision/:id` (the right target). **Loop-back closes:** board `board-esc-inbox` → `#/decision/DEC-CAST-412-04` (ESC_ATOM derived via `influenced.includes('CAST-417')`) → "escalated to me →" `escalateToBoard()` sets `boardFilter='human'` → `#/board`. Ticket PR link (`ticket-prlink` → `#/goal/CAST-412` `data-replay=execution`), onboarding dial link (`#/decisions/CAST-412`), reqs↔canvas entry (`reqs-canvas-link` → `#/reqs/CAST-412`), marketplace "Hire for a capability →" (`#/hire`) all present; trail↔decision↔ticket-chip IDs match by construction (all sourced from `ORG.decisions`; REST chip = DEC-CAST-412-02). **(2) Drift grep (Phase 3/4 set extended with CAST-417 · PR 2341 · 99.4 · 312 · 6 candidates · 12 contracts · 8-agent chain · the 5 dimension names · Priya Kannan · cast-export-csv):** CLEAN — every canonical string originates from `org.js`; all literal hits are either `#/kit` `FIXTURES` (the one sanctioned exception: #2341/99.4/312/99.9/505/M04), comments, or ORG-query predicates (CAST-417 as a data-value match anchoring ESC_ATOM, consistent with `FEATURE_ID` which is itself ORG-derived). **One drift FIX:** the `cast-export-csv` hardcoded fallback in `SkillsNew` (`demo.slug || 'cast-export-csv'`, plus a duplicate blurb literal) — `demo` already reads from `ORG.org.skills`; neutralized both fallbacks to generic non-canonical placeholders so no canonical string is duplicated outside `org.js`. **(3) Slop gate on six surfaces** (board · ticket log · CAST-417 escalation · stack-ranked report · agent résumé · reqs-doc) — delegated `/cast-preso-check-visual` + `/cast-preso-check-tone`, full-autonomy directive + no-browser static posture propagated verbatim. **Visual: 5 PASS + 1 code-fixable soft-flag** — the `EscalationRail` three options read too symmetric (B vs C distinguished only by solid-vs-dashed 1px border); FIXED in code (ghost option gets tighter `padding:7px 14px` + smaller 18px rank circle + 12px label, making a real hero>outline>ghost weight ladder; reinforces the existing 7A ranking, no new design). **Tone: all six "probably human"** — zero GPT-isms / hedging / formulaic cadence; the only systemic residue is em-dashes, which **fold into the EXISTING owner-approved standing de-em-dash carry-forward** (deferred unified pass, non-blocking — NOT a new flag). **5 pixel-dependent visual carry-forwards** (non-blocking, human eyeball): rework-meter proportions (ticket); dashed-vs-solid rail-border contrast (escalation); RadarChart 8px label legibility (report); Sparkline+Radar readability (résumé); `reqs-node--anchored` inset-shadow on cream-deep (reqs). **(4) Script + state sanity:** `SCRIPTS.hiring` beats (steps 1→2→3→4→5) and the wizard Next/Back (`setHireStep`, clamps at 5) drive the SAME `appState.hiring.step` → synced by construction; NO persistence (no localStorage/sessionStorage) → dial/filters/disclosures all reset on reload; a `/tmp` logic harness (never committed) confirmed ORG byte-identical after simulating every handler (filters, dial, reqs toggles, hiring steps). **Cleanup:** removed the now-dead `RouteStub`+`STUB_META` and `BoardStub` (every route resolves to a real view; `.stub-*`/`.board-frame` CSS left in place, harmless — Phase 6 strips kit chrome). `node --check` clean on the extracted module (final); **vt- anchors 6×1** (no duplicate-anchor regression from the ten routes); closed 5-op set intact (no sixth op); `org.js` NEVER touched (`git diff` still the exact 5.0 batch: 537 ins / 2 del); NO TESTS. **Human-eyeball carry-forward (non-blocking, no live browser):** open `prototype/index.html` and run the high-level Phase-5 arc as one click-through — board (filter chips hide the right cards; escalation inbox) → ticket (activity log + inline M04/S03/R02 + rework 1/3 + PR link; maker/checker avatars now open résumés) → "next › decision" → CAST-417 artifact (escalation rail, nothing pre-selected, now a legible 3-tier ladder) → "escalated to me" loops to a human-filtered board; hire funnel both ways through all 5 steps; flip the dial Balanced↔Conservative (dial_demo L2 promotes to a pinned stop, reload resets); plus the 5 pixel carry-forwards above + a unified de-em-dash pass.
