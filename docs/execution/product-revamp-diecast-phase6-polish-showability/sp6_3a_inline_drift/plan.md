# Sub-phase 6.3a: Distributable — Single-File Inline + Disk Smoke Test (parallel with 6.3b)

> **Pre-requisite:** Read
> `docs/execution/product-revamp-diecast-phase6-polish-showability/_shared_context.md` before
> starting this sub-phase. The binding constraints there are not optional.

## Objective

One file — `prototype/dist/diecast-prototype.html` — contains the **entire prototype** (markup,
styles, scripts, ORG data, base64 E1 rasters) and runs every flow when double-clicked from disk, with
only the locked CDN libraries and fonts fetched from the network. The final cross-surface fake-data
drift sweep is clean with **zero** sanctioned exceptions. On the critical path (6.1 → 6.2 → **6.3a** →
6.4).

## Dependencies
- **Requires completed:** Sub-phase **6.2** (inline **after** content is final — never before, or
  every fix happens twice).
- **Independent of 6.3b** (truly parallel-safe — 6.3b writes only the v2 map doc and touches **no**
  `index.html`; this is the first genuinely-parallel group in the project — see the file-collision
  honesty note in `_manifest.md`).
- **Assumed codebase state:** `prototype/index.html` is content-final (post-6.2); `prototype/dist/`
  and `prototype/_build/inline.mjs` do **not** exist yet (this sub-phase creates them);
  `prototype/assets/` holds `e1-acceptance.png` + `fr017-parity-three-tiers.png`.

## Estimated effort
0.5–0.75 session (~2h).

## Scope
**In scope:** writing `prototype/_build/inline.mjs`; emitting `prototype/dist/diecast-prototype.html`;
the final consolidated drift sweep on the dev files; retiring the `#/kit` fixture exception; the disk
smoke test (incl. a network-tab pass).
**Out of scope (do NOT do these):** any new surface/component/op; the SC-006 map (6.3b); vendoring CDN
ES modules into the file (CDN stays CDN); any test file/suite/harness; touching the Phase 4
canvas/parity sections beyond the read-only drift spot-check.

## Files to Create/Modify
| File | Action | Current State |
|------|--------|---------------|
| `prototype/_build/inline.mjs` | Create | Does not exist — zero-dep, build-time-only Node one-shot (the 2a generator precedent) |
| `prototype/dist/diecast-prototype.html` | Create (generated) | Does not exist — emitted by `inline.mjs`; header-marked; never hand-edited |
| `prototype/index.html` | Modify (drift-sweep fixes only) | Content-final post-6.2; 6.3a neutralizes any retyped canonical literal + completes the `#/kit` data swap |

## Detailed Steps / Key Activities

### Step 6.3a.1: Write `prototype/_build/inline.mjs`
A **zero-dependency, build-time-only Node one-shot** (the Phase 2a generator precedent; **never part
of the runtime or dev loop**) that reads `prototype/index.html` and emits
`prototype/dist/diecast-prototype.html`:
- Replace `<script src="data/org.js"></script>` with the file's content inlined as a **classic script
  block** (match on the **exact `src` attribute**, not a generic regex over all scripts).
- Replace each `<img src="assets/…">` with a **base64 data-URI** (E1 + FR-017 parity rasters). **Size
  guard:** if the emitted file exceeds **~5MB**, recompress rasters to **WebP ≤250KB each** and re-run.
- Prepend a header comment: `<!-- GENERATED by prototype/_build/inline.mjs — edit
  prototype/index.html, then re-run. -->`. **Idempotent:** re-running regenerates from dev files.
- 2b's CSS/SVG `onerror` raster fallbacks **stay in the markup** (inert once inlined — harmless, and
  they keep the dev file resilient).
- **Error posture:** **refuse to emit if an expected `src` isn't found** (mirrors the 2a generator's
  refuse-on-violation posture). No error-handling beyond that.

### Step 6.3a.2: CDN deps stay CDN
The import-map preact/htm/driver.js, the driver.css `<link>`, and Google Fonts **stay CDN** in the
dist. "Opens from disk" has meant "**one local file + https CDN**" since Phase 1's file:// constraint
analysis — **vendoring ES modules into the file without a bundler is brittle and re-introduces exactly
the toolchain FR-001 forbids.** Documented tradeoff: the file needs network for ~15KB of libraries +
fonts; a fully-offline variant (one-shot `vite-plugin-singlefile`, per the playbook, outside the dev
loop) is noted as **available but not built** (Decision 10).

### Step 6.3a.3: Final fake-data drift sweep (the 2a canon re-run, on the dev files, post-6.2)
Consolidated grep set = **Phase 3 base** (CAST-412 "Add RBAC to checkout" · M04/S03/R02 · 99.9 · 505 ·
rework 1/3 · crud-orchestrator) + **Phase 4 additions** (CAST-452 · CAST-461 · 180ms · 1h40m · 8% ·
source names) + **Phase 5 additions** (CAST-417 · canon PR # · 99.4 · 312 · 6 candidates · 12
contracts · 8-agent chain · 5 dimension names · PM persona name · cast-export-csv). Every canonical
literal in `index.html` must come from `ORG` rendering, **not retyped strings** — eyeball each grep
hit; **new Phase 6 copy** (tours, demo script, chooser) is checked for goal-title/id consistency
against `ORG.goals`.

### Step 6.3a.4: Retire the `#/kit` exception
2b's fixture literals were "the one sanctioned grep exception until its data swap" — **complete the
swap** (kit fixtures read `ORG`) if execution hasn't already, so the final sweep passes with **zero
exceptions**. `#/kit` **ships inside the dist** (hidden, hash-only, harmless) rather than being
stripped — stripping buys nothing and complicates the inliner (Decision 11).

> **NO ORG batch:** completing the swap means making `#/kit` **read from `ORG`** via the render path —
> not hand-editing `org.js`. If `org.js` genuinely lacks a value the kit needs (it should not — Phase 5
> already swept this), that is a flag, not a hand-edit; resolve via the generator only, and note it.

### Step 6.3a.5: Disk smoke test
Per the verification paragraph below, including a **network-tab pass** confirming no local-path
requests escaped inlining.

## Verification

> **NO TESTS (binding):** every check below is **manual click-through / static observation**. In an
> autonomous run with no browser, satisfy each via the strongest static evidence (`node --check` of
> both the dev module and the emitted dist module, grep audits, a throwaway `/tmp` logic harness that
> is never committed, byte-diff of two `inline.mjs` runs for idempotency) and record a non-blocking
> human-eyeball carry-forward for any rendered-pixel item. **Do not flag missing tests.** The inliner
> is **packaging tooling, not a test harness.**

**Verification (manual, from disk) — verbatim from the plan:** Double-click
`prototype/dist/diecast-prototype.html` →
- all five scenarios run end-to-end; the morph plays; E1 rasters render (**network tab shows requests
  only to the pinned CDNs + fonts — no `assets/` or `data/` requests**);
- tours + demo overlay work; reduced-motion fallback works; console clean;
- **file size recorded and ≤ ~5MB**; the dist header comment marks it generated.
- Dev `prototype/index.html` still works unchanged.
- The **drift grep returns hits only from the ORG data block**; lorem/em-dash sweep returns zero.

### Success Criteria (binary — every item must pass or carry forward with reason)
- [ ] `prototype/_build/inline.mjs` exists, zero-dep, build-time-only; matches exact `src` attributes;
      refuses to emit on a missing expected `src`; **idempotent** (two runs byte-identical).
- [ ] `prototype/dist/diecast-prototype.html` runs all five scenarios + morph + tours + demo overlay +
      reduced-motion from disk; console clean; header comment present; **≤ ~5MB** (size recorded).
- [ ] Network-tab (or static equivalent): **no `assets/` or `data/` requests** — only the pinned CDNs
      + fonts (`org.js` inlined as classic script; rasters base64 data-URIs); **no `fetch` introduced.**
- [ ] Dev `prototype/index.html` still works unchanged (relative `<img>` + onerror fallback intact).
- [ ] Final consolidated drift grep clean — **every canonical string originates from `ORG`**; the
      `#/kit` exception **RETIRED** (zero sanctioned exceptions); lorem/em-dash sweep zero.
- [ ] `#/kit` ships inside the dist (hidden, hash-only); `node --check` clean on both modules; no new
      op; no new SCRIPTS key; `generate-org.mjs` untouched (no Phase 6 batch).

## Design review (verbatim from the plan)
- ⚠ **Generated-file hygiene:** the dist file will tempt hand-edits during demo prep. The header
  comment + "regenerate, never edit" rule in activities is the guard.
- ⚠ **Inliner brittleness:** matching exact `src` attributes (not greedy regex) and the
  re-run-idempotent design keep the ~60-line script from corrupting markup; the smoke test is the
  backstop. No error-handling beyond "refuse to emit if an expected src isn't found" (mirrors the 2a
  generator's refuse-on-violation posture).
- **file:// legality re-verified post-inline** — base64 data-URIs and inline classic scripts are
  exactly the file://-legal forms Phase 1 established; the inliner introduces no `fetch`. ✓
- **NO TESTS compliance:** the inliner is packaging tooling (2a precedent), not a test harness; its
  only check is refuse-on-missing-src. ✓

### Design Review Flags (this sub-phase's rows, verbatim from the plan's consolidated table)
| Sub-phase | Flag | Action |
|-----------|------|--------|
| 6.3a | Dist file invites hand-edits | Generated-file header + regenerate-never-edit rule |
| 6.3a | Inliner could corrupt markup | Exact-src matching, refuse-on-missing-src, disk smoke test backstop |

## Execution Notes
- **Base64 rasters balloon the dist (Key Risk):** ~5MB guard + WebP recompression step; E1 + the
  FR-017 parity image are the only raster sets in the prototype.
- **CDN-offline tradeoff (Key Risk):** documented with a named, **not-built** fallback
  (`vite-plugin-singlefile`); the owner knows the file needs network for ~15KB of libs + fonts before
  any showing.
- **Tour copy drift (Key Risk):** Phase 6 copy is explicitly included in this drift sweep; titles
  render from `ORG.goals` wherever possible.
- **Parallel with 6.3b — genuinely safe:** 6.3a edits `index.html` + creates `_build/`/`dist/`; 6.3b
  creates ONLY `docs/plan/product-revamp-diecast-v2-surface-goal-map.md`. **Disjoint files** — no
  serialization needed (the first time in the project this is true).
- **Spec-linked files:** none — greenfield (FR-020); no `/cast-update-spec`.
- **Plan review:** SKIPPED per run config — do not dispatch `/cast-plan-review` or any reconciliation
  pass.
- **Failure policy (critical path):** 6.3a is a critical-path node — a second failure is
  **stop-and-report**.
- **Record borderline calls:** if the `#/kit` swap or any inliner decision required a judgment call,
  append a numbered entry to `docs/plan/product-revamp-diecast-borderline-calls.md` (continuing from
  #15).
