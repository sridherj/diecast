# data_analysis — authored source edits (Sub-phase 1a spike)

Three surgical edits to the corpus source, isolated so the *unchanged* surface stays large.
Full edited source: `new.collab.md`. Deterministic changed-set (from `block_diff.summarize`): `changed-set.json`.

block_diff counts: {'added': 0, 'modified': 1, 'removed': 1, 'unchanged': 2}

## Edits

### modified FR/section body
- **find:** A live capture showed the API returning 2 running while the database held 7.
- **replace:** A live capture showed the API returning 1 running while the database held 7.

### added SC / bullet
- **find:** - **The signature is diagnostic.**
- **replace:** - **The reaper never fires on idle panes.** The monitor's terminal-output contract has no idle-timeout branch, so a session parked at a sele

### removed bullet
- **find:** - **[DEFERRED]** Whether the status API's `running` filter should be reconciled to the raw DB count, or whether the divergence is intentiona
- **replace:** (deleted)

## Changed-set items

- modified [evidence] Evidence: ## Evidence - **The launch predicate is slot-bounded.** `_dispatcher_loop` only…
- removed [open_question] - **[DEFERRED]** Whether the status API's `running` filter should be reconciled…: - **[DEFERRED]** Whether the status API's `running` filter should be reconciled…
