# Spec Registry

All product specs for Diecast.

| Spec File | Feature | Module | Scope | Status | Version |
|-----------|---------|--------|-------|--------|---------|
| `cast-delegation-contract.collab.md` | cast-delegation-contract | cast-runtime | Parent-child file-based delegation contract: output-file naming, contract-v2 schema reference, terminal status set, polling backoff, idle-timeout, heartbeat-by-mtime, atomic write, RUN_ID-scoped path validation, test hooks | Draft | 1 |
| `cast-output-json-contract.collab.md` | cast-output-json-contract | cast-runtime | Contract-v2 schema for `<goal_dir>/.agent-run_<RUN_ID>.output.json`: field-by-field types, allowed status set, artifacts[] item shape, per-agent extension placeholder | Draft | 1 |
| `cast-init-conventions.collab.md` | cast-init-conventions | cast-init | File conventions for cast-* artifacts: authorship suffixes (`.human.md`/`.ai.md`/`.collab.md`), date prefixes (`YYYY-MM-DD-<slug>.md`), `_v2` versioning rule, `<goal_name>` flat-vs-folder heuristic, per-agent default write paths. Source of truth referenced by `/cast-init`'s `CLAUDE.md` template. | Draft | 1 |
| `cast-ui-testing.collab.md` | cast-ui-testing | cast-server | UI e2e test harness contract: every screen covered, mandatory test updates on UI change, zero-residue re-runnable, test agents invisible in dev. Linked plan: `docs/plan/2026-05-01-cast-ui-e2e-test-harness.collab.md`. | Draft | 2 |
| `cast-runs-screen.collab.md` | cast-runs-screen | cast-server | Threaded `/runs` page contract: `get_runs_tree` rollups, rework detection, ctx thresholds, HTMX poll-safety on `.run-status-cells`, collapse persistence, depth cap, action-button macro response shape, canonical `/jobs/{id}?include=children` sub-tree fetch, removed legacy `/children` and `/row` endpoints. Linked plan: `docs/plan/2026-05-01-cast-runs-threaded-tree.collab.md`. | Draft | 1 |
| `cast-user-invocation-tracking.collab.md` | cast-user-invocation-tracking | cast-server | User-typed /cast-* slash commands captured as top-level agent_run rows; close-by-session_id with 1h staleness window. Linked plan: `docs/plan/2026-05-01-capture-user-invocations-as-runs.collab.md`. Hook install: see `cast-hooks.collab.md`. | Draft | 1 |
| `cast-hooks.collab.md`                    | cast-hooks                    | cast-server | Polite-citizen install/uninstall contract for Claude Code settings.json. Additive merge, atomic write, idempotent dedup, surgical uninstall, third-party preservation. Hook commands written as absolute paths through `~/.claude/skills/diecast/bin/cast-hook` (gstack-pattern install seam — no PATH dependency). Reference pattern: gstack-settings-hook. | Draft | 2 |
| `cast-subagent-and-skill-capture.collab.md` | cast-subagent-and-skill-capture | cast-server | Hook-driven capture of Task()-dispatched cast-* subagents and PreToolUse Skill events; session_id + claude_agent_id parent resolution / closure; goal_slug inheritance; system-ops auto-seed; per-event matcher support in cast-hook installer; fire-and-forget POST. Linked plan: `docs/plan/2026-05-01-cast-subagent-and-skill-capture.collab.md`. | Draft | 1 |
| `cast-goal-classification.collab.md` | cast-goal-classification | cast-server | Refine Requirements v2 classification contract Phases 3a/3b cite: the LOCKED nine-value `WorkFamily` enum, the `classification.*` front-matter schema (field-by-field), the code-side confidence gate (`GATE_SILENT`/`GATE_CONFIRM`, auto/confirm/choose + headless policy), the two-level `--family` checker, `FAMILY_RECIPES`/`RECIPE_REALIZATION` block-recipe semantics, and the add-a-family checklist + `taxonomy_version` bump (with the Phase 3b routing-home extension appended). States the classifier subagent sits outside the delegation + output-json contracts. Source of truth: `cast-server/cast_server/requirements_render/families.py`. Linked plan: `docs/plan/2026-06-11-refine-requirements-v2-phase2-classification.md`. | Draft | 1 |
| `cast-requirements-render.collab.md` | cast-requirements-render | cast-server | Refine Requirements v2 HTML-render contract Phase 4 cites: the `GET /goals/{slug}/render` page route (lazy hash-keyed regen, prompt-to-begin on no/stub requirements, 404 + path-traversal kill, 500-leaves-html-intact), the generated `refined_requirements.html` **render-class** artifact (AUTO-GENERATED header + `source-hash`, read-only, authorship-suffix-exempt — records but does not extend `cast-init-conventions`), the pure `render_requirements(parsed, *, version) -> RenderResult` boundary, the `extract_zero_click_view` + `bin/cast-render-zero-click` (exit 2) contract, the `cast-requirements-checker` verdict schema + binary PASS rule (`can_state_what` + `missing[]`, never `score`) and its outside-`cast-delegation-contract` subagent carve-out, and the **Phase 4 DOM contract** (contiguous semantic selectable units under real headings; NO `id=`/`data-block-anchor` — superseded stable-IDs not reintroduced). **v2 (Phase 4 extension):** the same-door comment API at `/api/goals/{slug}/requirements` (negotiated `POST/GET …/comments` dual-assertion parity, `author_kind` the only human/agent distinction, the resolve/reopen/relocate/orphan state machine with `409`/`422` guards + relocate verbatim-substring backstop), `comment_service` flat-fn naming contract, `requirement_version_service.create_next` (snapshot+archive in one txn, carry-forward by omission, `displaced_comment_ids` string-find), the convergence rule (`unconverged` iff `open_comment_count > 0`), the pure `block_diff.diff_blocks`/`summarize` cross-phase engine + `/changes` byte-equality, the `/goals/{slug}/render/diff` tracked-changes route with the **transient-`id` exception** (scoped to the diff view only), the derived read-time `displaced` model + surfaced tray (save path untouched — decision #1), the `cast-comment-reanchor` bare-JSON verdict carve-out (orphan-over-guess, decision #9), the FR-011 single-requirements-file folder invariant, and the progressive-enhancement script note (file:// degrades to read-only). **v3:** the FR-028 select-to-comment affordance (JS-injected behind the slug guard, paired cast-ui-testing harness update). **v4 (Phase 3 maker pipeline — happy-path inversion):** the render is produced by the `cast-requirements-what` → `cast-requirements-how` two-agent maker pipeline (tool-free `claude -p` subagents, `--tools ""` makes "never writes the canonical `.collab.md`" structural) driven by `render_job_service` through the named stages `run_what → gate_what → run_how → gate_html → publish`; `render_requirements()`/`rerender_requirements_html` demoted to the **fallback branch** (served only on literal no-output); the **generating-state route** (`GET …/render` starts a single-flight background job + serves a live generating state polling `GET …/render/status` and swapping in on `ready`; cached/stub/404/comment paths never wait); the **logical non-DOM id backbone** (canonical ids emitted verbatim once as anchor labels, US7 zero-`id` DOM contract preserved) + the **verbatim-carriage clause** (`maker_gate.check_html` via the shared `container_text_index` walker); determinism scope narrowed to the fallback substrate + cache envelope; and the **structural-violation owner override** (gate exhaustion → flagged best-attempt + `served-by: structural_violation` stamp + "needs review" badge, NOT the deterministic page — surface, don't suppress). **v5 (Phase 4a quality gate — completes the v4 forward pointers):** the happy path is now gated by **one** agent `cast-requirements-render-checker` (`model: opus`, subagent bare-JSON carve-out) grading comprehension **and** visual quality in a single pass; the binary PASS + ranking score are computed **code-side** in the pure `checker_verdict.py` (`derive_pass`/`canonical_score`, the agent `score` float advisory only); the **quality-driven rework loop** (`run_checker → decide_quality` inserted between `gate_html` and `publish`) reworks-until-bar with provenance-tagged feedback + WHAT-escalation, bounded **only** by the `QUALITY_MAX_ATTEMPTS` anti-infinite-loop ceiling (never a cost/latency/tier cap); the **two-branch fallback policy AS THE OWNER OVERRIDE** (deterministic page ONLY on literal no-output; a structurally-broken-but-present attempt is scoreable/servable + flagged, never the silent swap; non-convergence serves the best attempt PREFER-VALID-THEN-SCORE with `non_convergent`/`checker_unavailable`/`structural_degradation`/`structural_violation` reasons); and the four additive `render_jobs` human-review flag columns (`human_review`/`review_reason`/`published_attempt`/`published_score`) + the status-JSON `human_review` read from the served-artifact envelope — flag **recording-only**, the flagged-renders LIST deferred to Phase 5d. Linked files (v1): `routes/pages.py`, `requirements_render/renderer.py`, `requirements_render/zero_click.py`, `services/requirements_render_service.py`, `agents/cast-requirements-checker/`; (v2) `routes/api_requirements.py`, `services/comment_service.py`, `services/requirement_version_service.py`, `requirements_render/block_diff.py`, `requirements_render/diff_render.py`, `static/requirements_comments.js`, `templates/fragments/requirements_comments/`, `agents/cast-comment-reanchor/`, `tests/ui/`; (v4) `agents/cast-requirements-what/`, `agents/cast-requirements-how/`, `requirements_render/maker_gate.py`, `services/render_job_service.py`, `requirements_render/templates/generating.html.j2`; (v5) `agents/cast-requirements-render-checker/`, `requirements_render/checker_verdict.py`, `config.py`, `db/schema.sql`, `tests/eval_quality_gate.py`, `tests/fixtures/quality_gate/`. **v6 (Phase 4b — comments & versions survive the maker):** the **comment-survival gate** `check_comment_survival` (pure, in `maker_gate.py`, reusing the single `container_text_index` walker — no copy) classifying in-block (MUST place; a miss is a witnessed verbatim-carriage violation) vs cross-boundary (recorded, never a violation) misses, `gate_html` widened to fetch open comments per-entry and merge in-block violations into the SAME structural channel; the **DECISION #10 OVERRIDE recorded override-aware** (a survival-failing in-block attempt is flagged + servable via `structural_violation`, NOT blocking and NOT the deterministic fallback — which fires only on literal no-output) + the read-time `.comment-unplaced` tray badge covering both miss classes; the `cast-comment-reanchor` **contract-v2 backward-compatible superset** (optional `change_set` + per-comment block-context inputs, a `narration` output, the third `resolved` verdict with `relocated > resolved > orphaned`-when-unsure bias + anchor-pickability, model tier unchanged `sonnet`, still the bare-JSON subagent carve-out); the **stored-once diff narration** (`version_diff_narrations` table + `save_narration`/`get_narration`, same-door `POST …/versions/{head}/narration` recomputing `summarize(diff_blocks)` server-side and validating every note key to a deterministic item → **422 all-or-nothing**, UNIQUE-upsert, size caps), `GET …/changes` gaining a `narration` sibling while `counts`/`items` stay byte-for-byte (FR-024 re-scoped), the panel attaching notes by `(change, heading_or_ref)` and rendering LLM text through the **autoescaped** template only (a diff can never show a change absent from the source); SC-003 proven e2e via `eval_sc003_survival.py` (same-source = zero DB changes + survival green; source-edit loop = zero new orphans beyond the deleted block + relocated mark placed; trust-boundary check). Linked files (v6): `agents/cast-refine-requirements/`, `tests/test_comment_survival.py`, `tests/eval_reanchor.py`, `tests/eval_sc003_survival.py`, `tests/test_schema_migration.py` (existing `maker_gate.py`/`render_job_service.py`/`requirement_version_service.py`/`api_requirements.py`/`db/schema.sql`/`requirements_comments.js`/`_theme.css.j2`/`changes_panel.html`/`agents/cast-comment-reanchor/` widened in place). Linked plans: `docs/plan/2026-06-11-refine-requirements-v2-phase3a-html-render.md`, `docs/plan/2026-06-11-refine-requirements-v2-phase4-annotation-versioning.md`, `docs/plan/2026-06-12-refine-requirements-v3-phase3-maker-pipeline.md`, `docs/plan/2026-06-12-refine-requirements-v3-phase4a-quality-gate.md`, `docs/plan/2026-06-12-refine-requirements-v3-phase4b-comment-survival.md`. **v7 (Phase 5 — gap-fill: ask upstream, never fabricate):** the activated `gaps[]` WHAT-doc field + the `GAPS-DETECTED` HOW trailer (the bounded HOW-asks-WHAT round-trip), the tool-free `cast-requirements-gapfill` grounded-or-refuse subagent over a goal-own-artifacts corpus allowlist, server-side `validate_evidence` (reusing the single `verbatim_locate` — "never fabricates" enforced, not promised), gap reconciliation through the **byte-unchanged** v2 change-request gate under the goal's **GATE-ALL** policy, the class-based `.rr-gap` page marker (question + one fixed status string, NEVER the proposed answer) with un-mark-by-regeneration, the nine-family verification record (via `eval_family_sweep.py`), and the minimal read-only **flagged-renders list** on `/runs` (`list_flagged_renders` over the 4a recording-only flag columns — resolving the v6 Phase-5d pointer). Linked files (v7): `agents/cast-requirements-gapfill/`, `tests/test_gap_reconciliation.py`, `tests/fixtures/family_corpus/`, `tests/eval_family_sweep.py` (existing `maker_gate.py`/`render_job_service.py`/`config.py`/`routes/pages.py`/`templates/pages/runs.html` widened in place). Linked plan: `docs/plan/2026-06-12-refine-requirements-v3-phase5-gapfill-signoff.md`. **v8 (Phase-5 follow-up — HOW two-mode + render-snapshot anchoring):** the HOW maker's **CREATE/UPDATE two-mode contract** (`decide_mode` — CREATE re-renders fresh + readability-first; UPDATE is the **deterministic splice** keeping unchanged unit containers byte-identical and re-rendering only changed-block `RR-FRAGMENT`s; Spike-1a verdict FAIL → splice, so the published UPDATE artifact is **server-assembled**; `check_update_fidelity` on NORMALIZED container text; massive-change/oversize/family-flip/flagged-prior all degrade to CREATE, noted, never errored) with the two `RENDER_UPDATE_*` knobs (the `RENDER_UPDATE_ENABLED` flag-gate **retired**); **comment anchoring MOVED source→published-render snapshot** (`requirement_comments` gains additive `block_ref`/`anchor_space`; `block_ref` server-resolved by `comment_anchor.resolve_render_anchor`, **never client-supplied**; ref-less-render NULL = placed SUCCESS; displacement + survival reorient to render space); **US16 blanket verbatim-carriage SUPERSEDED** (anchor labels + one-unit-one-container survive; CREATE leaf-text copy-exact dropped for readability, with the **empty-shell** `check_html` gate as the new CREATE floor); `cast-comment-reanchor` **contract v3** (additive render-space context; one publish-boundary dispatch for an UPDATE's expected misses); **gap-CR idempotency under UPDATE** (reuse prior `gaps-state.json`, skip `emit_change_requests`); the nine-family record now **9/9 clean**. Known limitation recorded: paraphrase meaning-drift (fidelity checker OUT under HOLD). Linked files (v8): `requirements_render/block_splice.py`, `requirements_render/comment_anchor.py`, `tests/test_render_mode_decision.py`, `tests/test_comment_anchor_render.py`, `tests/test_maker_gate_update_fidelity.py`, `tests/test_maker_gate_empty_shell.py`, `tests/test_comment_service.py`, `agents/cast-comment-reanchor/` (v3) (existing `maker_gate.py`/`render_job_service.py`/`config.py`/`db/schema.sql`/`comment_service.py`/`api_requirements.py`/`agents/cast-requirements-how/`/`agents/cast-requirements-what/`/`tests/eval_sc003_survival.py`/`tests/eval_family_sweep.py` widened in place). Linked plans: `docs/plan/2026-06-12-refine-requirements-v3-how-update-mode-render-anchoring.md`, `docs/plan/2026-06-13-refine-requirements-v3-spec-v8-change-brief.md`. | Draft | 8 |
| `cast-workflow-routing.collab.md` | cast-workflow-routing | cast-server | Refine Requirements v2 routing contract future per-family pipeline goals cite: the `WORKFLOW_REGISTRY`/`WORKFLOW_FAMILIES` registry (every value a `stub` in v2), the pure-and-total `resolve` + idempotent `record_routing_decision` (`WorkflowHandle` shape, `implemented`/`stub`/`unmatched`/`needs-classification` status set), the three `goals` columns (`workflow_family`/`routing_handle`/`routed_at`) + `goal.yaml` render, the `{family}:{status}` stored-stamp format and its documented staleness (D1), `goals.workflow_family` as the authoritative routing record (D2), and the `POST /api/goals/{slug}/route` request/response + recording rule (`unmatched`/`needs-classification` never persist). Cites `cast-goal-classification.collab.md` for the family vocabulary + the single add-a-family checklist; notes `/route` is a plain JSON API outside the delegation + output-json contracts. Source of truth: `cast-server/cast_server/services/workflow_router_service.py`. Linked plan: `docs/plan/2026-06-11-refine-requirements-v2-phase3b-workflow-router.md`. | Draft | 1 |
| `cast-requirements-roundtrip.collab.md` | cast-requirements-roundtrip | cast-server | Refine Requirements v2 round-trip write-back contract Phase-5-and-later goals cite: "propose + notify + gate, never auto-sync" — the same-door `POST /api/goals/{slug}/change-requests` intake (one handler, `author_type` data not branch, server-derived anti-spoof identity), the integer `base_version` reference shape, the single global `WRITEBACK_GATE_POLICY` graduated-trust gate (`gate-except-additions` default), the `proposed→applied\|conflicted\|rejected\|superseded` lifecycle + append-only `change_request_events`, the pure/total `detect_conflict` (`clean\|conflicted\|orphaned`) region-by-quote predicate + 3-way `ConflictSurface` (no auto-merge), the `cast-requirements-writeback` sole-writer surgical-splice carve-out (no whole-file overwrite path; path-scoped), and the transactional `notifications_outbox` + relay (`recent_writebacks` deduped on `change_request_id`, 0-lost/0-dup). REFERENCES `cast-requirements-render.collab.md` (change-summary engine, `create_next`, `{convergence, open_comment_count}` surface) and `cast-delegation-contract.collab.md` (file-apply carve-out) — does not duplicate them. The four owner decisions stated verbatim. Source of truth: `cast-server/cast_server/services/change_request_service.py`, `services/notification_service.py`, `requirements_render/conflict.py`, `routes/change_requests.py`. Linked plan: `docs/plan/2026-06-11-refine-requirements-v2-phase5-roundtrip-writeback.md`. **v2 (Phase 5d — first real downstream emitter):** render gap-fill (`render_job_service.emit_change_requests`) proposes a missing detail as a `kind="addition"` CR through this contract's intake — **emitter-side only**, the intake/gate/conflict/sole-writer-apply/outbox/relay all consumed byte-unchanged (every roundtrip test green); the "real emitters deferred" Out-of-Scope fence narrowed to record this one emitter. | Draft | 2 |
