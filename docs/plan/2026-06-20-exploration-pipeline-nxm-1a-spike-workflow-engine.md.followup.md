# Sub-phase 1a — Post-execution review follow-ups

Generated by cast-subphase-runner B4. Classification: **coding** (Rule 1 — a `.py` with
executable logic, `toy_workflow.py`, was written and run). The reviewed code is **throwaway
spike code** in `goals/exploration-pipeline-nxm-claude-workflow-9010-angle/exploration/spike-1a/`.

## Review dispatch note (R.4)

`cast-review-code` launches an **independent reviewer in a new terminal tab** (tmux/dispatch).
In this autonomous subagent run, that live launch carries the same flake risk flagged in the
spike's own environment caveat (tmux/dispatch launches flake headless). Rather than risk a flaky
tab dispatch on ~230 lines of throwaway code, the runner performed a **focused inline self-review**
of the load-bearing logic (cap/queue + isolation verifier) and records the findings below.
**To run the full external review manually:** `/cast-review-code goals/exploration-pipeline-nxm-claude-workflow-9010-angle/exploration/spike-1a/toy_workflow.py`.

## Findings (bumped to followup — medium confidence, judgment-required, not single-Edit auto-fixable)

1. **`observed_max_concurrent` is computed, not measured** (`toy_workflow.py:280`,
   `= min(len(cells), cap)`). It is an upper-bound *assertion*, not a true runtime peak-concurrency
   measurement. **The honest cap evidence is the E4 wall-clock** (24 cells × 0.3s sleep finishing in
   2.71s ⇒ ~4 capped waves, not a single over-subscribed wave). Impact: **none on the verdict** —
   the wall-clock is the real proof; the JSON field is just a label. *If 3a reuses any of this,
   replace the computed field with an instrumented peak counter (atomic increment/decrement around
   each cell) so the cap claim is measured.*

2. **Dead `simulate()` helper** (`toy_workflow.py`, the standalone `simulate()` function). `main()`
   uses its own inline `ThreadPoolExecutor` loop so nonces match the verifier; `simulate()` is no
   longer called. Throwaway-code cleanliness only; safe to delete. Not auto-removed (multi-line
   deletion, not a single-Edit patch).

Neither finding blocks the decision gate or the PARTIAL→VIABLE route. Both are recorded for the
G1 reviewer / Phase 3a, not for action in this spike.
